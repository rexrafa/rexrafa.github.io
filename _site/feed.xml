<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.9.5">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-05-08T12:57:48+02:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Rafa Cardoso’s Tech Blog</title><subtitle>Insights on AWS, DevOps, and AI</subtitle><author><name>Rafa Cardoso</name></author><entry><title type="html">Back to the Basics: DNS Resolution</title><link href="http://localhost:4000/2026/05/08/dns-resolution/" rel="alternate" type="text/html" title="Back to the Basics: DNS Resolution" /><published>2026-05-08T00:00:00+02:00</published><updated>2026-05-08T00:00:00+02:00</updated><id>http://localhost:4000/2026/05/08/dns-resolution</id><content type="html" xml:base="http://localhost:4000/2026/05/08/dns-resolution/"><![CDATA[<p>Every engineer eventually gets bitten by DNS. A service starts timing out, and three hours later you realize your pod has been hammering the per-ENI 1024-PPS link-local budget. Or you spend an afternoon debugging a stale record only to discover the browser was happily ignoring your perfectly configured <code class="language-plaintext highlighter-rouge">resolv.conf</code>. DNS is invisible when it works and devastating when it doesn’t.</p>

<p>So this is a ground-up walk through how DNS actually behaves across the Linux stack — the OS resolver, Docker containers, Kubernetes pods on EKS, and the AWS-level constraints that quietly shape all of it. References to primary sources throughout.</p>

<hr />

<h2 id="what-is-dns">What Is DNS?</h2>

<p>DNS is the distributed directory that maps human-readable names to IP addresses. When your app calls <code class="language-plaintext highlighter-rouge">connect("api.example.com", 443)</code>, the OS has to resolve that name before a single TCP packet leaves the box. Resolution is delegated to a hierarchy: root nameservers → TLD nameservers → authoritative nameservers.</p>

<p>Here’s the full recursive path for a cold cache:</p>

<pre><code class="language-mermaid">sequenceDiagram
    participant App as Application
    participant OS as OS Resolver&lt;br/&gt;(resolv.conf)
    participant Rec as Recursive Resolver&lt;br/&gt;(VPC DNS)
    participant Root as Root NS
    participant TLD as TLD NS (.com)
    participant Auth as Authoritative NS&lt;br/&gt;(example.com)

    Note over App,Auth: Cold cache: full recursive resolution

    App-&gt;&gt;OS: getaddrinfo("api.example.com")
    OS-&gt;&gt;Rec: Query UDP:53 api.example.com A?

    Rec-&gt;&gt;Root: Who handles .com?
    Root--&gt;&gt;Rec: Referral: .com NS records

    Rec-&gt;&gt;TLD: Who handles example.com?
    TLD--&gt;&gt;Rec: Referral: example.com NS records

    Rec-&gt;&gt;Auth: api.example.com A?
    Auth--&gt;&gt;Rec: A 93.184.216.34 TTL 300

    Rec--&gt;&gt;OS: Answer 93.184.216.34 (cached for TTL)
    OS--&gt;&gt;App: IP 93.184.216.34

    Note over Rec: Recursive resolver caches the answer for TTL seconds.&lt;br/&gt;Subsequent queries are served from cache (no upstream hops).
</code></pre>

<blockquote>
  <p><strong>Reference:</strong> <a href="https://www.rfc-editor.org/rfc/rfc1034">RFC 1034 – Domain Names: Concepts and Facilities</a>, <a href="https://www.rfc-editor.org/rfc/rfc1035">RFC 1035 – Domain Names: Implementation and Specification</a></p>
</blockquote>

<hr />

<h2 id="linux-etcresolvconf-and-the-os-resolver">Linux: <code class="language-plaintext highlighter-rouge">/etc/resolv.conf</code> and the OS Resolver</h2>

<p>On Linux, name resolution comes down to two files. <code class="language-plaintext highlighter-rouge">nsswitch.conf</code> defines the resolution order (files, DNS, mDNS…), and <code class="language-plaintext highlighter-rouge">/etc/resolv.conf</code> configures the DNS resolver itself. The DNS part is what matters here.</p>

<p>A typical <code class="language-plaintext highlighter-rouge">resolv.conf</code> on an EC2 instance inside a VPC:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nameserver 10.0.0.2      # VPC base CIDR + 2; the same resolver is also reachable at 169.254.169.253
search ec2.internal
options timeout:2 attempts:3
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">nameserver</code> is the resolver to query. Worth knowing: the glibc resolver hard-codes <code class="language-plaintext highlighter-rouge">MAXNS = 3</code> in <code class="language-plaintext highlighter-rouge">resolv.h</code>, so anything beyond the third entry is silently ignored. This is also why an AWS DHCP option set with four DNS servers will only ever exercise the first three on a Linux box. <a href="https://www.man7.org/linux/man-pages/man5/resolv.conf.5.html">[1]</a> <a href="https://docs.aws.amazon.com/whitepapers/latest/hybrid-cloud-dns-options-for-vpc/constraints.html">[2]</a></p>

<p><code class="language-plaintext highlighter-rouge">search</code> is the list of domain suffixes appended when resolving a short hostname — <code class="language-plaintext highlighter-rouge">curl myservice</code> becomes a query for <code class="language-plaintext highlighter-rouge">myservice.ec2.internal</code> before the bare name is tried. The list itself is also bounded (<code class="language-plaintext highlighter-rouge">MAXDNSRCH = 6</code> entries / 256 chars in glibc). Then there’s <code class="language-plaintext highlighter-rouge">ndots</code>, a threshold (default 1) for how many dots a name needs before it’s tried as absolute first. That one becomes critical in Kubernetes — more on it below. Finally <code class="language-plaintext highlighter-rouge">timeout</code>/<code class="language-plaintext highlighter-rouge">attempts</code> control per-server query timeout (default 5s) and total attempts across all listed nameservers (default 2). The <code class="language-plaintext highlighter-rouge">timeout:2 attempts:3</code> you see above is the standard AWS tuning to fail fast under packet loss.</p>

<h3 id="udp-vs-tcp">UDP vs. TCP</h3>

<p>DNS speaks <strong>UDP port 53</strong> by default. Classic DNS caps UDP payloads at 512 bytes (RFC 1035 §2.3.4), so anything larger gets truncated and the client is expected to retry over <strong>TCP port 53</strong>. Modern resolvers use EDNS0 (RFC 6891) to negotiate UDP payloads up to 4096 bytes, but TCP is still the fallback.</p>

<p>A few things still <em>require</em> TCP no matter what: zone transfers (AXFR/IXFR) are always TCP; responses bigger than the negotiated EDNS0 buffer get the TC (truncated) bit set and force a TCP retry; and DNSSEC responses are often large enough to push you over the edge.</p>

<p>You can poke at this on Linux with <code class="language-plaintext highlighter-rouge">dig +tcp api.example.com</code> to force TCP, or <code class="language-plaintext highlighter-rouge">tcpdump -i any port 53</code> to see the actual mix on the wire.</p>

<blockquote>
  <p><strong>Reference:</strong> <a href="https://www.rfc-editor.org/rfc/rfc1035#section-4.2.2">RFC 1035 §4.2.2 – Messages Carried by TCP</a>, <a href="https://www.rfc-editor.org/rfc/rfc6891">RFC 6891 – EDNS0</a>, <a href="https://www.rfc-editor.org/rfc/rfc7766">RFC 7766 – DNS Transport over TCP</a>, <a href="https://www.man7.org/linux/man-pages/man5/resolv.conf.5.html">resolv.conf(5)</a></p>
</blockquote>

<hr />

<h2 id="browser-dns-the-cache-you-forget-exists">Browser DNS: The Cache You Forget Exists</h2>

<p>Browsers maintain their own DNS cache, <strong>completely independent</strong> of the OS resolver. Chrome and Chromium-based browsers (Edge included) keep an in-process cache with a default TTL cap. You can poke at it via <code class="language-plaintext highlighter-rouge">chrome://net-internals/#dns</code>.</p>

<p>That independence is what catches people out. Once an entry is in the browser’s cache, the OS resolver isn’t consulted again until that entry expires — restarting <code class="language-plaintext highlighter-rouge">systemd-resolved</code> or running <code class="language-plaintext highlighter-rouge">resolvectl flush-caches</code> does nothing for in-flight browser sessions.</p>

<p>The TTL situation is also weirder than you’d expect. When Chromium uses the system <code class="language-plaintext highlighter-rouge">getaddrinfo</code> path it has no way to learn the upstream TTL (POSIX <code class="language-plaintext highlighter-rouge">getaddrinfo</code> doesn’t expose it), so positive entries get pinned at a flat <strong>60 seconds</strong> and negative entries at <strong>5 seconds</strong>. When Chromium’s own async resolver / DoH path is in use it honours the wire TTL but still enforces a <strong>60 second minimum</strong>. The upshot: even if the authoritative NS returns <code class="language-plaintext highlighter-rouge">TTL=86400</code>, the browser’s effective TTL is much shorter — and a <code class="language-plaintext highlighter-rouge">TTL=1</code> response is still held for at least a minute. <a href="https://textslashplain.com/2022/03/31/chromiums-dns-cache/">[1]</a> <a href="https://chromium.googlesource.com/chromium/src/+/main/net/dns/README.md">[2]</a></p>

<p>One more thing worth knowing: browsers always send FQDNs to the OS resolver. Search domains are completely ignored — short-name resolution via <code class="language-plaintext highlighter-rouge">search</code> is a shell concern, not a browser concern.</p>

<p>Firefox does the same, with its own cache configurable under <code class="language-plaintext highlighter-rouge">about:config</code> → <code class="language-plaintext highlighter-rouge">network.dnsCacheExpiration</code> (default 60 seconds, capped by <code class="language-plaintext highlighter-rouge">network.dnsCacheExpirationGracePeriod</code>).</p>

<blockquote>
  <p><strong>Reference:</strong> <a href="https://chromium.googlesource.com/chromium/src/+/main/net/dns/README.md">Chromium net/dns README</a>, <a href="https://source.chromium.org/chromium/chromium/src/+/main:net/dns/host_resolver_manager.cc">Chromium HostResolverManager source</a>, <a href="https://textslashplain.com/2022/03/31/chromiums-dns-cache/">Chromium’s DNS Cache (textslashplain)</a>, <a href="https://developer.mozilla.org/en-US/docs/Web/Performance/dns-prefetch">MDN: DNS prefetching</a></p>
</blockquote>

<hr />

<h2 id="docker-which-dns-does-my-container-use">Docker: Which DNS Does My Container Use?</h2>

<p>Docker adds its own DNS layer on top, and the behavior depends entirely on which network driver the container uses.</p>

<h3 id="user-defined-bridge-networks">User-defined bridge networks</h3>

<p>On a <strong>user-defined</strong> bridge (anything created with <code class="language-plaintext highlighter-rouge">docker network create</code>), Docker rewrites the container’s <code class="language-plaintext highlighter-rouge">/etc/resolv.conf</code> to point at <strong><code class="language-plaintext highlighter-rouge">127.0.0.11</code></strong> — Docker’s embedded DNS server, listening on a per-namespace loopback. That embedded resolver does two things: it answers service discovery queries within the network (Docker injects A/AAAA records for container names and aliases, so a container named <code class="language-plaintext highlighter-rouge">db</code> is resolvable from its peers as <code class="language-plaintext highlighter-rouge">db</code>), and it forwards everything else to the DNS servers from the <strong>host’s</strong> <code class="language-plaintext highlighter-rouge">/etc/resolv.conf</code> snapshot taken at container start.</p>

<p>That last bit is the gotcha. Docker copies the host DNS config at container <strong>start time</strong>. If the host’s <code class="language-plaintext highlighter-rouge">resolv.conf</code> changes later — say, a VPN connects or disconnects — existing containers won’t notice until they’re restarted.</p>

<blockquote>
  <p>Note: the <strong>default</strong> <code class="language-plaintext highlighter-rouge">bridge</code> network (the one named <code class="language-plaintext highlighter-rouge">bridge</code> that’s created automatically) has no embedded DNS. Only user-defined bridges do. On the default bridge, container-to-container name resolution needs the legacy <code class="language-plaintext highlighter-rouge">--link</code> flag, which writes <code class="language-plaintext highlighter-rouge">/etc/hosts</code> entries instead of using DNS. Docker’s own docs recommend user-defined networks for anything non-trivial.</p>
</blockquote>

<h3 id="host-network---network-host">Host network (<code class="language-plaintext highlighter-rouge">--network host</code>)</h3>

<p>No network namespace, no embedded DNS. The container shares the host’s network stack and reads <code class="language-plaintext highlighter-rouge">/etc/resolv.conf</code> directly from the host. The <code class="language-plaintext highlighter-rouge">127.0.0.11</code> resolver is nowhere in the picture.</p>

<h3 id="custom-dns-flags">Custom DNS flags</h3>

<p>You can override DNS per-container or globally:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Per container</span>
docker run <span class="nt">--dns</span> 8.8.8.8 <span class="nt">--dns-search</span> example.com myimage

<span class="c"># Global daemon default (daemon.json)</span>
<span class="o">{</span>
  <span class="s2">"dns"</span>: <span class="o">[</span><span class="s2">"10.0.0.2"</span><span class="o">]</span>,
  <span class="s2">"dns-search"</span>: <span class="o">[</span><span class="s2">"corp.internal"</span><span class="o">]</span>
<span class="o">}</span>
</code></pre></div></div>

<p>When <code class="language-plaintext highlighter-rouge">--dns</code> is set, Docker writes it into the container’s <code class="language-plaintext highlighter-rouge">/etc/resolv.conf</code> and the embedded DNS server forwards to it.</p>

<pre><code class="language-mermaid">flowchart TD
    A[Container process] --&gt;|"getaddrinfo()"| B["/etc/resolv.conf\n127.0.0.11"]
    B --&gt; C{Docker embedded DNS}
    C --&gt;|"known container\nor alias"| D[Return container IP\nfrom Docker network]
    C --&gt;|"unknown name"| E[Forward to host\nresolv.conf DNS]
    E --&gt; F[VPC DNS / 8.8.8.8]
    F --&gt;|answer| E
    E --&gt;|answer| C
    C --&gt;|answer| A

    style C fill:#3b82f6,color:#fff
    style D fill:#10b981,color:#fff
    style F fill:#f59e0b,color:#fff
</code></pre>

<blockquote>
  <p><strong>Reference:</strong> <a href="https://docs.docker.com/network/drivers/bridge/#dns-services">Docker docs – Container DNS</a>, <a href="https://docs.docker.com/network/network-tutorial-standalone/">Docker embedded DNS server</a></p>
</blockquote>

<hr />

<h2 id="kubernetes--eks-dns-all-the-way-down">Kubernetes / EKS: DNS All the Way Down</h2>

<p>Kubernetes DNS is where it gets genuinely complex. Three layers stack on top of each other: Pod configuration, CoreDNS, and (optionally) NodeLocal DNSCache.</p>

<h3 id="layer-1-pod-etcresolvconf-and-dnspolicy">Layer 1: Pod <code class="language-plaintext highlighter-rouge">/etc/resolv.conf</code> and <code class="language-plaintext highlighter-rouge">dnsPolicy</code></h3>

<p>When kubelet starts a Pod, it writes a <code class="language-plaintext highlighter-rouge">/etc/resolv.conf</code> inside the container based on the Pod’s <code class="language-plaintext highlighter-rouge">dnsPolicy</code> and <code class="language-plaintext highlighter-rouge">dnsConfig</code>. The default policy is <code class="language-plaintext highlighter-rouge">ClusterFirst</code>, which produces:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nameserver 10.100.0.10      # CoreDNS ClusterIP
search &lt;namespace&gt;.svc.cluster.local svc.cluster.local cluster.local
options ndots:5
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ndots:5</code> is the biggest operational footgun in Kubernetes DNS. With it set, any name with <strong>fewer than 5 dots</strong> is first tried with each search domain appended, and only as an absolute name on the <em>last</em> attempt. For <code class="language-plaintext highlighter-rouge">api.example.com</code> (2 dots), that’s <strong>4 sequential DNS lookups</strong> — three NXDOMAINs followed by the correct answer:</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">api.example.com.&lt;namespace&gt;.svc.cluster.local.</code> → NXDOMAIN</li>
  <li><code class="language-plaintext highlighter-rouge">api.example.com.svc.cluster.local.</code> → NXDOMAIN</li>
  <li><code class="language-plaintext highlighter-rouge">api.example.com.cluster.local.</code> → NXDOMAIN</li>
  <li><code class="language-plaintext highlighter-rouge">api.example.com.</code> → answer</li>
</ol>

<p>And because glibc’s <code class="language-plaintext highlighter-rouge">getaddrinfo</code> fires A and AAAA queries in parallel by default (since glibc 2.9), the resolver actually emits <strong>8 UDP packets</strong> per call. Keep that number in mind when we get to the per-ENI packet budget below. The reason <code class="language-plaintext highlighter-rouge">ndots:5</code> is the Kubernetes default at all is to keep SRV-style relative lookups (<code class="language-plaintext highlighter-rouge">_proto._tcp.svc</code>) resolvable inside the cluster zone. <a href="https://pracucci.com/kubernetes-dns-resolution-ndots-options-and-why-it-may-affect-application-performances.html">[1]</a></p>

<p>You can cut the amplification two ways. Either use FQDNs with a trailing dot (<code class="language-plaintext highlighter-rouge">api.example.com.</code>) in your application config — the trailing dot makes the name <em>fully qualified</em> and skips the search list entirely — or just lower <code class="language-plaintext highlighter-rouge">ndots</code> per-pod:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">spec</span><span class="pi">:</span>
  <span class="na">dnsConfig</span><span class="pi">:</span>
    <span class="na">options</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">ndots</span>
        <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">2"</span>
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">dnsPolicy</code> field is what drives all of this:</p>

<table>
  <thead>
    <tr>
      <th><code class="language-plaintext highlighter-rouge">dnsPolicy</code></th>
      <th>resolv.conf behavior</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ClusterFirst</code> (default)</td>
      <td>Cluster DNS (CoreDNS) + search domains</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ClusterFirstWithHostNet</code></td>
      <td>Same, but for pods with <code class="language-plaintext highlighter-rouge">hostNetwork: true</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Default</code></td>
      <td>Inherits the node’s <code class="language-plaintext highlighter-rouge">/etc/resolv.conf</code> verbatim</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">None</code></td>
      <td>Fully custom via <code class="language-plaintext highlighter-rouge">dnsConfig</code> — you provide everything</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p><strong>Reference:</strong> <a href="https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/">Kubernetes docs – DNS for Services and Pods</a>, <a href="https://kubernetes.io/docs/reference/kubernetes-api/workload-resources/pod-v1/#PodSpec">Kubernetes dnsConfig reference</a></p>
</blockquote>

<h3 id="layer-2-coredns">Layer 2: CoreDNS</h3>

<p>CoreDNS is the cluster DNS server. It runs as a Deployment in <code class="language-plaintext highlighter-rouge">kube-system</code> and is exposed via a ClusterIP Service (typically <code class="language-plaintext highlighter-rouge">10.100.0.10</code> on EKS). Every pod with <code class="language-plaintext highlighter-rouge">ClusterFirst</code> dnsPolicy sends its queries here.</p>

<p>It works through a plugin chain configured via the <code class="language-plaintext highlighter-rouge">Corefile</code> ConfigMap. The critical zones for EKS:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># Corefile (simplified EKS default)
.:53 {
    errors
    health
    kubernetes cluster.local in-addr.arpa ip6.arpa {
        pods insecure
        fallthrough in-addr.arpa ip6.arpa
    }
    prometheus :9153
    forward . /etc/resolv.conf    # forwards non-cluster queries to node's DNS (VPC DNS)
    cache 30
    loop
    reload
    loadbalance
}
</code></pre></div></div>

<p>Anything for <code class="language-plaintext highlighter-rouge">*.cluster.local</code> is answered straight from the Kubernetes API (Services, Pods) — no upstream hop. Everything else is forwarded to the node’s <code class="language-plaintext highlighter-rouge">/etc/resolv.conf</code>, which on EKS points to the VPC DNS resolver at VPC CIDR +2.</p>

<pre><code class="language-mermaid">sequenceDiagram
    participant Pod
    participant CoreDNS
    participant K8sAPI as Kubernetes API
    participant VPC_DNS as VPC DNS (169.254.169.253)

    Pod-&gt;&gt;CoreDNS: Query: myservice.default.svc.cluster.local
    CoreDNS-&gt;&gt;K8sAPI: Lookup Service "myservice" in "default"
    K8sAPI--&gt;&gt;CoreDNS: ClusterIP 10.100.12.34
    CoreDNS--&gt;&gt;Pod: A 10.100.12.34 TTL 30

    Pod-&gt;&gt;CoreDNS: Query: s3.amazonaws.com
    CoreDNS-&gt;&gt;VPC_DNS: Forward query
    VPC_DNS--&gt;&gt;CoreDNS: A 52.92.x.x TTL 60
    CoreDNS--&gt;&gt;Pod: A 52.92.x.x TTL 30
</code></pre>

<blockquote>
  <p><strong>Reference:</strong> <a href="https://docs.aws.amazon.com/eks/latest/userguide/coredns.html">EKS – DNS and service discovery</a>, <a href="https://coredns.io/plugins/kubernetes/">CoreDNS Kubernetes plugin</a></p>
</blockquote>

<h3 id="layer-3-nodelocal-dnscache">Layer 3: NodeLocal DNSCache</h3>

<p>At scale, having every pod on a node fire UDP queries at a single CoreDNS ClusterIP creates two problems. First, conntrack table churn — each new UDP “connection” creates a new conntrack entry with a 30-second timeout. Second, latency: the CoreDNS pod answering you might be on a different node entirely.</p>

<p><strong>NodeLocal DNSCache</strong> fixes both. A <code class="language-plaintext highlighter-rouge">node-cache</code> DaemonSet runs on every node, listening on a <strong>link-local IP</strong> (typically <code class="language-plaintext highlighter-rouge">169.254.20.10</code>) on an interface with no conntrack. Pods are reconfigured to query this local IP instead of CoreDNS directly. The flow changes fundamentally:</p>

<pre><code class="language-mermaid">sequenceDiagram
    participant Pod
    participant NodeCache as NodeLocal DNSCache&lt;br/&gt;(169.254.20.10)
    participant CoreDNS as CoreDNS Service&lt;br/&gt;(10.100.0.10)
    participant VPC_DNS as VPC DNS

    Note over Pod,NodeCache: Pod resolv.conf: nameserver 169.254.20.10

    Pod-&gt;&gt;NodeCache: UDP query: myservice.default.svc.cluster.local

    alt Cache HIT
        NodeCache--&gt;&gt;Pod: Answer from local cache (sub-ms)
    else Cache MISS – cluster name
        NodeCache-&gt;&gt;CoreDNS: TCP query (no conntrack race!)
        CoreDNS--&gt;&gt;NodeCache: Answer
        NodeCache--&gt;&gt;Pod: Answer (now cached)
    else Cache MISS – external name
        NodeCache-&gt;&gt;VPC_DNS: UDP query (via node's resolv.conf)
        VPC_DNS--&gt;&gt;NodeCache: Answer
        NodeCache--&gt;&gt;Pod: Answer (now cached)
    end
</code></pre>

<p>A few things going on here are worth unpacking.</p>

<p>The pod → cache hop skips conntrack entirely. The node-local DNS pod programs <code class="language-plaintext highlighter-rouge">iptables -t raw … -j NOTRACK</code> rules for traffic to/from <code class="language-plaintext highlighter-rouge">169.254.20.10:53</code> (both UDP and TCP). To unpack why that matters: <strong>conntrack</strong> is the Linux kernel module that tracks every network “connection” passing through a node — including UDP “flows,” even though UDP itself is connectionless. It’s what lets <code class="language-plaintext highlighter-rouge">iptables</code> rewrite a Service ClusterIP into a real Pod IP (DNAT) and remember the rewrite so reply packets get translated back. The catch is that when several UDP packets from the same socket leave the box at nearly the same instant — exactly what happens when glibc fires A and AAAA queries in parallel — they can race each other into conntrack. Two packets try to insert the same flow entry at the same time, one of them loses, and the kernel drops it. The application’s resolver doesn’t see a reply, waits out its <strong>5-second timeout</strong>, and retries — which is the origin of the infamous “DNS is randomly slow by exactly 5 seconds” reports. Because NodeLocal traffic is marked <code class="language-plaintext highlighter-rouge">NOTRACK</code>, no conntrack entry is ever created for the pod → cache hop, so the race simply can’t happen there. See <a href="https://github.com/kubernetes/kubernetes/issues/56903">kubernetes/kubernetes#56903</a> and Weaveworks’ “Racy conntrack” post for the full kernel-level write-up.</p>

<p>The cache → CoreDNS hop is upgraded to TCP. By default the node-local agent talks to the upstream <code class="language-plaintext highlighter-rouge">kube-dns</code> Service over TCP, reusing a small pool of long-lived connections. TCP entries leave conntrack as soon as the FIN handshake completes, instead of waiting out the default <code class="language-plaintext highlighter-rouge">nf_conntrack_udp_timeout</code> of 30s — which dramatically shrinks the conntrack table on busy nodes. <a href="https://kubernetes.io/docs/tasks/administer-cluster/nodelocaldns/">[2]</a></p>

<p>Negative caching is re-enabled. Quick refresher: an <strong>NXDOMAIN</strong> is the DNS response code that means “this name does not exist” — the resolver checked, and there’s no such record, anywhere. <strong>Negative caching</strong> is when the resolver remembers those “doesn’t exist” answers for a while, instead of re-asking upstream every single time. That matters in Kubernetes because <code class="language-plaintext highlighter-rouge">ndots:5</code> makes the resolver walk through three or four made-up names (<code class="language-plaintext highlighter-rouge">api.example.com.&lt;namespace&gt;.svc.cluster.local.</code>, then <code class="language-plaintext highlighter-rouge">…svc.cluster.local.</code>, then <code class="language-plaintext highlighter-rouge">…cluster.local.</code>) before it tries the real one — and each of those wrong guesses comes back as NXDOMAIN. Without negative caching, every call from every pod re-asks upstream for those same non-existent names. NodeLocal DNSCache caches the NXDOMAINs locally, so after the first miss those wrong guesses are answered instantly from the node’s cache and the upstream “NXDOMAIN storm” collapses into a single round-trip after warmup.</p>

<p>Failover is health-checked. The agent listens on the local IP via a dummy interface and also binds to the kube-dns ClusterIP virtually, so if the agent dies, the kernel routes pod queries straight to the kube-dns Service — degraded but still functional.</p>

<p>It’s been stable since Kubernetes v1.18, promoted to GA in <a href="https://github.com/kubernetes/enhancements/issues/1024">KEP-1024</a>. Enabled by default on GKE, available as an addon on EKS.</p>

<p>The Pod’s <code class="language-plaintext highlighter-rouge">resolv.conf</code> after enabling NodeLocal DNSCache looks like:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nameserver 169.254.20.10    # NodeLocal cache, not CoreDNS directly
search default.svc.cluster.local svc.cluster.local cluster.local
options ndots:5
</code></pre></div></div>

<p>To enable it on EKS, deploy the <code class="language-plaintext highlighter-rouge">node-local-dns</code> DaemonSet with the appropriate kube-dns ClusterIP substituted:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">kubedns</span><span class="o">=</span><span class="si">$(</span>kubectl get svc kube-dns <span class="nt">-n</span> kube-system <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">={</span>.spec.clusterIP<span class="o">}</span><span class="si">)</span>
<span class="nv">localdns</span><span class="o">=</span><span class="s2">"169.254.20.10"</span>
<span class="nv">domain</span><span class="o">=</span><span class="s2">"cluster.local"</span>

curl <span class="nt">-Lo</span> nodelocaldns.yaml <span class="se">\</span>
  https://raw.githubusercontent.com/kubernetes/kubernetes/master/cluster/addons/dns/nodelocaldns/nodelocaldns.yaml

<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s/__PILLAR__LOCAL__DNS__/</span><span class="nv">$localdns</span><span class="s2">/g"</span> nodelocaldns.yaml
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s/__PILLAR__DNS__DOMAIN__/</span><span class="nv">$domain</span><span class="s2">/g"</span>  nodelocaldns.yaml
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s/__PILLAR__DNS__SERVER__/</span><span class="nv">$kubedns</span><span class="s2">/g"</span> nodelocaldns.yaml

kubectl apply <span class="nt">-f</span> nodelocaldns.yaml
</code></pre></div></div>

<p>One footnote on rollout: if (and only if) kube-proxy is running in <code class="language-plaintext highlighter-rouge">ipvs</code> mode, you also need to flip the kubelet <code class="language-plaintext highlighter-rouge">--cluster-dns</code> flag on each node to <code class="language-plaintext highlighter-rouge">169.254.20.10</code> and restart the kubelet. In the default <code class="language-plaintext highlighter-rouge">iptables</code> mode you can skip that step — the agent binds to <em>both</em> <code class="language-plaintext highlighter-rouge">169.254.20.10</code> and the kube-dns ClusterIP simultaneously, and the NOTRACK rules ensure traffic to the ClusterIP is intercepted locally without DNAT.</p>

<blockquote>
  <p><strong>Reference:</strong> <a href="https://kubernetes.io/docs/tasks/administer-cluster/nodelocaldns/">Kubernetes – Using NodeLocal DNSCache</a>, <a href="https://github.com/kubernetes/kubernetes/issues/56903">kubernetes/kubernetes#56903</a></p>
</blockquote>

<h3 id="eks-auto-mode">EKS Auto Mode</h3>

<p>EKS Auto Mode changes where CoreDNS lives in a meaningful way. Instead of running as a centralized Deployment of pods in <code class="language-plaintext highlighter-rouge">kube-system</code>, Auto Mode runs CoreDNS as a <strong>systemd service on every worker node</strong>, alongside the VPC CNI and EBS CSI driver. DNS queries from pods always go to the node-local CoreDNS instance — effectively giving you the same locality benefit as NodeLocal DNSCache, but managed entirely by AWS. Port 53 is reserved on Auto Mode nodes, which means if you try to deploy the upstream <code class="language-plaintext highlighter-rouge">node-local-dns</code> DaemonSet, the pods will fail to bind and crash-loop.</p>

<p>The <code class="language-plaintext highlighter-rouge">ndots:5</code> amplification and the 1024 PPS per-ENI limit are unchanged — those are properties of the pod’s <code class="language-plaintext highlighter-rouge">resolv.conf</code> and the EC2 instance, not of how CoreDNS is packaged. But because every node has its own local CoreDNS, the conntrack race and cross-node hop latency issues from Layer 3 above are already addressed by the architecture itself.</p>

<blockquote>
  <p><strong>Reference:</strong> <a href="https://docs.aws.amazon.com/eks/latest/userguide/auto-networking.html">EKS Auto Mode – Networking</a>, <a href="https://repost.aws/articles/ARGrPcCxZETRCNh9ktPhM_RA/how-do-i-troubleshoot-dns-in-amazon-eks-auto-mode">Troubleshoot DNS in Amazon EKS Auto Mode (re:Post)</a>, <a href="https://docs.aws.amazon.com/eks/latest/userguide/managing-coredns.html">Manage CoreDNS for DNS in Amazon EKS</a></p>
</blockquote>

<hr />

<h2 id="aws-dns-the-limits-that-will-find-you-in-production">AWS DNS: The Limits That Will Find You in Production</h2>

<h3 id="the-1024-pps-per-eni-hard-limit">The 1024 PPS per ENI Hard Limit</h3>

<p>This is the single most operationally important DNS constraint on AWS. Every ENI attached to an EC2 instance or Lambda function has a <strong>hard limit of 1,024 packets per second</strong> to AWS link-local services. Crucially, that’s an <em>aggregate</em> budget shared across Route 53 Resolver DNS queries (<code class="language-plaintext highlighter-rouge">169.254.169.253</code>, <code class="language-plaintext highlighter-rouge">fd00:ec2::253</code>, or VPC CIDR <code class="language-plaintext highlighter-rouge">+2</code>), Instance Metadata Service calls to <code class="language-plaintext highlighter-rouge">169.254.169.254</code>, Amazon Time Sync NTP at <code class="language-plaintext highlighter-rouge">169.254.169.123</code>, and Windows licensing activation traffic on Windows AMIs.</p>

<p>You can’t raise the quota. Excess packets are silently dropped at the host, and your application just sees DNS timeouts and retries.</p>

<blockquote>
  <p>“Each network interface for an EC2 instance can send a maximum of 1024 packets per second to Route 53 Resolver. […] This quota applies to the combined total of DNS queries, IMDS, NTP, and Windows licensing activation traffic to link-local addresses.” — <a href="https://docs.aws.amazon.com/whitepapers/latest/hybrid-cloud-dns-options-for-vpc/constraints.html">AWS Hybrid Cloud DNS – Constraints</a></p>
</blockquote>

<p>1024 PPS is easier to hit than it sounds. A Kubernetes node with dozens of pods doing external DNS resolution under <code class="language-plaintext highlighter-rouge">ndots:5</code> produces up to 8 UDP packets per <code class="language-plaintext highlighter-rouge">getaddrinfo</code> call (4 search-list permutations × A+AAAA). About <strong>128 application-level lookups per second</strong> per node is enough to saturate the budget — and IMDSv1/v2 chatter from your sidecars and SDKs eats into the same pool.</p>

<p>It’s worth being explicit about what <em>doesn’t</em> fix this: the 1024 PPS limit is enforced at the <strong>instance ENI</strong>, on packets destined for link-local addresses. You can’t lift it by adding Route 53 Resolver endpoints, by switching to a bigger instance type, or by pointing at a different VPC DNS IP. As long as the instance is sending to <code class="language-plaintext highlighter-rouge">169.254.169.253</code> (or VPC CIDR +2), the host-level rate limiter is what counts the packets. The actual mitigations are all about <em>not sending so many packets in the first place</em>:</p>

<ul>
  <li><strong>NodeLocal DNSCache</strong> is the big one. Most queries are served from a per-node cache and never hit the link-local resolver at all; misses are forwarded to CoreDNS over a persistent TCP connection, which amortizes the per-packet cost and avoids the conntrack churn UDP causes.</li>
  <li><strong>Lower <code class="language-plaintext highlighter-rouge">ndots</code></strong> (or use FQDNs with a trailing dot for hot external names). This collapses the 4×-search-list amplification described above into a single query, which alone can cut DNS PPS by 4–8×.</li>
</ul>

<h3 id="route53-api-5-requestssecond">Route53 API: 5 Requests/Second</h3>

<p>The Route 53 <strong>management</strong> API — zone and record CRUD — is rate-limited to <strong>5 requests per second per AWS account</strong>, full stop, regardless of region. This is completely separate from the DNS query path and covers operations like <code class="language-plaintext highlighter-rouge">ChangeResourceRecordSets</code>, <code class="language-plaintext highlighter-rouge">ListResourceRecordSets</code>, <code class="language-plaintext highlighter-rouge">CreateHostedZone</code>, and friends. Exceed it and Route 53 returns HTTP <code class="language-plaintext highlighter-rouge">400</code> with <code class="language-plaintext highlighter-rouge">Code=Throttling</code> and <code class="language-plaintext highlighter-rouge">Message="Rate exceeded"</code>.</p>

<p>Two specific shapes that bite in production. First, <code class="language-plaintext highlighter-rouge">ChangeResourceRecordSets</code> has its own payload limits: a single change can’t contain more than <strong>1,000 <code class="language-plaintext highlighter-rouge">ResourceRecord</code> elements</strong> (and an <code class="language-plaintext highlighter-rouge">UPSERT</code> counts each record twice, so the practical cap is 500), and the sum of <code class="language-plaintext highlighter-rouge">Value</code> characters can’t exceed <strong>32,000 characters</strong> (also doubled for <code class="language-plaintext highlighter-rouge">UPSERT</code>). Bulk DNS migrations need to batch carefully. Second, <code class="language-plaintext highlighter-rouge">external-dns</code> and similar controllers — at scale, with hundreds of Services and Ingresses, the reconciliation loop can saturate the 5 req/s budget all by itself. Bumping <code class="language-plaintext highlighter-rouge">--interval</code>, using <code class="language-plaintext highlighter-rouge">--batch-change-size</code>, and enabling exponential-backoff retries usually does the trick.</p>

<p><a href="https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/DNSLimitations.html">[3]</a> <a href="https://repost.aws/knowledge-center/route-53-avoid-throttling-errors">[4]</a></p>

<h3 id="route53-resolver-inbound-and-outbound-endpoints">Route53 Resolver: Inbound and Outbound Endpoints</h3>

<p>Route53 Resolver is a managed DNS relay that lives inside your VPC, in two flavors. <strong>Inbound endpoints</strong> expose an ENI inside your VPC that accepts DNS queries <em>from</em> on-premises resolvers — this lets on-prem infrastructure resolve private hosted zone names like <code class="language-plaintext highlighter-rouge">rds.internal</code> without standing up a full split-horizon DNS. The on-prem resolver forwards to the inbound endpoint IP, which hands the query to the VPC DNS. <strong>Outbound endpoints</strong> go the other direction: they forward queries <em>from</em> your VPC out to on-premises resolvers, based on forwarding rules. Useful when your VPC workloads need to resolve on-prem names (<code class="language-plaintext highlighter-rouge">corp.internal</code>) that only exist outside AWS.</p>

<p>Both flavors come with caveats that affect latency and reliability. Endpoint ENIs are pinned to specific subnets and AZs — AWS requires at least two endpoint IPs across two AZs for redundancy, and if you under-provision, an AZ outage can still throttle resolution. Outbound resolver rules are matched against the queried name <em>before</em> the VPC’s default resolver answers, so a rule for <code class="language-plaintext highlighter-rouge">corp.internal</code> short-circuits the VPC DNS path entirely; a mistyped or overly broad rule (e.g., <code class="language-plaintext highlighter-rouge">internal</code> with <code class="language-plaintext highlighter-rouge">RECURSIVE</code> action) can black-hole queries for an entire suffix. Each resolver endpoint IP has its own per-IP query throughput (around 10,000 QPS) — AWS recommends scaling by adding endpoint IPs rather than leaning on a single 2-IP endpoint. Note that this is a separate budget from the 1024 PPS instance ENI limit discussed earlier; an endpoint helps on-prem ↔ VPC traffic, not your in-VPC workload’s per-instance packet budget.</p>

<pre><code class="language-mermaid">flowchart LR
    subgraph VPC
        P[Pod / EC2] --&gt;|DNS query| VPC_DNS[VPC DNS\n169.254.169.253]
        VPC_DNS --&gt;|"cluster.local\nPrivate Hosted Zone"| PHZ[Route53 Private\nHosted Zone]
        VPC_DNS --&gt;|"corp.internal rule"| OUT[Outbound Endpoint ENI]
        IN[Inbound Endpoint ENI] --&gt; VPC_DNS
    end

    subgraph On-Premises
        OnPrem[On-prem DNS\nResolver] --&gt;|"rds.internal query"| IN
        OUT --&gt;|"corp.internal query"| OnPrem
    end

    style VPC_DNS fill:#3b82f6,color:#fff
    style OUT fill:#10b981,color:#fff
    style IN fill:#10b981,color:#fff
    style PHZ fill:#6c63ff,color:#fff
</code></pre>

<blockquote>
  <p><strong>Reference:</strong> <a href="https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/resolver.html">Route53 Resolver</a>, <a href="https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/resolver-rules-managing.html">Resolver endpoints and rules</a></p>
</blockquote>

<h3 id="route53-dns-firewall">Route53 DNS Firewall</h3>

<p>Route53 DNS Firewall sits between your workload and the VPC DNS resolver, evaluating domain-based allow/block/override rules <strong>at the VPC DNS level</strong> before the query leaves your VPC.</p>

<p>Three things to watch out for in practice. The first is the BLOCK action itself: depending on the rule’s <code class="language-plaintext highlighter-rouge">BlockResponse</code> (<code class="language-plaintext highlighter-rouge">NODATA</code>, <code class="language-plaintext highlighter-rouge">NXDOMAIN</code>, or <code class="language-plaintext highlighter-rouge">OVERRIDE</code> with a synthetic CNAME / A record), the firewall returns the configured response instead of forwarding upstream. Applications see a failed lookup and typically log <code class="language-plaintext highlighter-rouge">NXDOMAIN</code> — or, for <code class="language-plaintext highlighter-rouge">NODATA</code>, a successful query with an empty answer set that some clients surface as <code class="language-plaintext highlighter-rouge">SERVFAIL</code>. If rule groups aren’t maintained carefully, legitimate domains get caught in the crossfire; the classic incident is a CDN moving its origin to a domain that happens to match a regex on a generic threat-feed list.</p>

<p>Second, fail-open vs. fail-close. The <code class="language-plaintext highlighter-rouge">FirewallFailOpen</code> setting per VPC defaults to <code class="language-plaintext highlighter-rouge">ALLOW</code> (fail-open) — if the firewall is unavailable or times out, queries are allowed through. Switch it to <code class="language-plaintext highlighter-rouge">DENY</code> (fail-close) and a Firewall outage blocks all DNS resolution in the VPC. That’s a reliability vs. security call you have to make explicitly.</p>

<p>Third, latency. DNS Firewall adds a small evaluation cost for every query — typically single-digit milliseconds, but on high-QPS workloads it compounds.</p>

<p>To see what’s actually happening, enable query logging and watch CloudWatch Logs. Each entry shows the action taken (ALLOW, BLOCK, OVERRIDE) and the matched rule group.</p>

<blockquote>
  <p><strong>Reference:</strong> <a href="https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/resolver-dns-firewall.html">Route53 DNS Firewall</a>, <a href="https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/resolver-dns-firewall-vpc-associating-rule-group.html">DNS Firewall fail-open setting</a></p>
</blockquote>

<hr />

<p>Thanks for reading — hope this made DNS feel a little less like a black box. If any of it saves you a three-hour debugging session some Tuesday afternoon, it was worth writing.</p>]]></content><author><name>Rafa Cardoso</name></author><category term="Network" /><category term="aws" /><category term="network" /><category term="DNS" /><category term="Linux" /><category term="Kubernetes" /><category term="EKS" /><category term="Docker" /><category term="Route53" /><category term="CoreDNS" /><summary type="html"><![CDATA[Every engineer eventually gets bitten by DNS. A service starts timing out, and three hours later you realize your pod has been hammering the per-ENI 1024-PPS link-local budget. Or you spend an afternoon debugging a stale record only to discover the browser was happily ignoring your perfectly configured resolv.conf. DNS is invisible when it works and devastating when it doesn’t.]]></summary></entry><entry><title type="html">Scaling Private Key Operations Across a Fleet of AWS Nitro Enclaves</title><link href="http://localhost:4000/2026/03/09/nitro-enclave-kms/" rel="alternate" type="text/html" title="Scaling Private Key Operations Across a Fleet of AWS Nitro Enclaves" /><published>2026-03-09T13:00:00+01:00</published><updated>2026-03-09T13:00:00+01:00</updated><id>http://localhost:4000/2026/03/09/nitro-enclave-kms</id><content type="html" xml:base="http://localhost:4000/2026/03/09/nitro-enclave-kms/"><![CDATA[<p>Recently I was working on a project where we needed to use AWS Nitro Enclaves as a Trusted Execution Environment (TEE) to handle highly sensitive cryptographic keys. The challenge was straightforward to describe but tricky to solve: we needed an asymmetric key pair that multiple enclave instances could share for encrypting and decrypting files, but the private key could <strong>never</strong> be accessible outside the enclave – not to the host machine, not to operators, not to anyone.</p>

<p>This post walks through the concepts and architecture I used to solve this problem, from the fundamentals of TEEs and Nitro Enclaves to a concrete pattern for generating, distributing, and recovering encryption keys across a fleet of enclaves.</p>

<hr />

<h2 id="what-is-a-trusted-execution-environment-tee">What is a Trusted Execution Environment (TEE)?</h2>

<p>A Trusted Execution Environment is an isolated area of a processor (or, in the cloud, an isolated virtual machine) that guarantees the confidentiality and integrity of the code and data loaded inside it. The key properties of a TEE are:</p>

<ul>
  <li><strong>Isolation</strong> – Code and data inside the TEE are protected from the rest of the system, including the operating system, hypervisor, and even users with root access.</li>
  <li><strong>Attestation</strong> – The TEE can cryptographically prove its identity to external parties. This means a remote service can verify <em>exactly</em> which code is running before releasing sensitive material to it.</li>
  <li><strong>Confidentiality</strong> – Data processed inside the TEE is not observable from the outside, even by the infrastructure operator.</li>
</ul>

<p>TEEs are the right tool when you need to process sensitive data – cryptographic keys, PII, financial records – and you want a hardware-backed guarantee that the host machine cannot peek at it. Traditional access control (IAM policies, firewalls) is necessary but not sufficient; a TEE adds a layer that doesn’t rely on trusting the operator or the host OS.</p>

<hr />

<h2 id="what-is-aws-nitro-enclaves">What is AWS Nitro Enclaves?</h2>

<p><a href="https://docs.aws.amazon.com/enclaves/latest/user/enclaves-user.html">AWS Nitro Enclaves</a> is Amazon’s implementation of a TEE on EC2. An enclave is a separate, hardened virtual machine that is created by partitioning memory and vCPUs from a parent EC2 instance. What makes enclaves special:</p>

<ul>
  <li><strong>No persistent storage</strong> – The enclave has no disk. All data lives in volatile memory and is destroyed when the enclave terminates.</li>
  <li><strong>No external networking</strong> – The enclave cannot make network calls. It communicates with the outside world <em>only</em> through a <a href="https://docs.aws.amazon.com/enclaves/latest/user/nitro-enclave-concepts.html">vsock</a> (virtual socket) connection to its parent EC2 instance.</li>
  <li><strong>No interactive access</strong> – You cannot SSH into an enclave. Not even root on the parent instance can access the enclave’s memory or processes.</li>
  <li><strong>Cryptographic attestation</strong> – The Nitro Hypervisor generates a signed attestation document that proves the enclave’s identity (which code is running). AWS KMS natively understands these attestation documents and can use them to gate access to cryptographic operations.</li>
</ul>

<p>Since the enclave has no network of its own, it relies on the parent EC2 instance to proxy requests to AWS services over vsock. This means all API traffic – requests and responses – passes through the host. However, the enclave’s memory is hardware-isolated by the Nitro Hypervisor, so the parent instance has no way to access the data once it is inside the enclave. The architecture is specifically designed so that sensitive material is always encrypted <em>before</em> it travels through this proxy (more on this in the <code class="language-plaintext highlighter-rouge">Recipient</code> flow later), and only the enclave holds the keys to decrypt it.</p>

<blockquote>
  <p>For a deeper dive into the concepts, see the <a href="https://docs.aws.amazon.com/enclaves/latest/user/nitro-enclave-concepts.html">Nitro Enclaves concepts</a> documentation.</p>
</blockquote>

<hr />

<h2 id="building-an-enclave-image">Building an Enclave Image</h2>

<p>An enclave runs from an <strong>Enclave Image File (EIF)</strong>, which is built from a Docker image using the <a href="https://docs.aws.amazon.com/enclaves/latest/user/cmd-nitro-build-enclave.html">Nitro CLI</a>. The process is similar to building any container image, with an extra conversion step:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Build your Docker image as usual</span>
docker build <span class="nt">-t</span> my-enclave-app:latest <span class="nb">.</span>

<span class="c"># Convert it to an EIF</span>
nitro-cli build-enclave <span class="se">\</span>
    <span class="nt">--docker-uri</span> my-enclave-app:latest <span class="se">\</span>
    <span class="nt">--output-file</span> my-enclave-app.eif
</code></pre></div></div>

<p>The build output includes a set of <strong>measurements</strong> – SHA384 hashes that uniquely identify the enclave image:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Enclave Image successfully created.
{
  "Measurements": {
    "HashAlgorithm": "Sha384 { ... }",
    "PCR0": "7fb5c55bc2ecbb68ed99a13d7122abfc0666b926a79d5379bc58b9445c84217f59cfdd36c08b2c79552928702efe23e4",
    "PCR1": "235c9e6050abf6b993c915505f3220e2d82b51aff830ad14cbecc2eec1bf0b4ae749d311c663f464cde9f718acca5286",
    "PCR2": "0f0ac32c300289e872e6ac4d19b0b5ac4a9b020c98295643ff3978610750ce6a86f7edff24e3c0a4a445f2ff8a9ea79d"
  }
}
</code></pre></div></div>

<h3 id="dont-ship-unsigned-images-to-production">Don’t Ship Unsigned Images to Production</h3>

<p>An unsigned EIF will work, but in production you want one more guarantee: that the image was built by <em>your</em> CI pipeline and not tampered with. Signing the EIF adds <code class="language-plaintext highlighter-rouge">PCR8</code> to the attestation document – a hash of your signing certificate that KMS can verify before releasing any key material. You can sign during the build or afterward using <a href="https://docs.aws.amazon.com/enclaves/latest/user/cmd-nitro-sign-eif.html"><code class="language-plaintext highlighter-rouge">nitro-cli sign-eif</code></a>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Generate an ECDSA P-384 private key (only ECDSA is supported for EIF signing)</span>
openssl ecparam <span class="nt">-name</span> secp384r1 <span class="nt">-genkey</span> <span class="nt">-out</span> signing-key.pem

<span class="c"># Create a CSR with SHA-384 (-nodes: no passphrase, -subj: customize for your org)</span>
openssl req <span class="nt">-new</span> <span class="nt">-key</span> signing-key.pem <span class="nt">-sha384</span> <span class="nt">-nodes</span> <span class="se">\</span>
    <span class="nt">-subj</span> <span class="s2">"/CN=MyOrg/C=US/ST=WA/L=Seattle/O=MyOrg/OU=Eng"</span> <span class="nt">-out</span> csr.pem

<span class="c"># Self-sign the CSR into an X.509 certificate (expired certs cause E36/E39/E11 errors)</span>
openssl x509 <span class="nt">-req</span> <span class="nt">-days</span> 365 <span class="nt">-in</span> csr.pem <span class="nt">-out</span> certificate.pem <span class="se">\</span>
    <span class="nt">-sha384</span> <span class="nt">-signkey</span> signing-key.pem

<span class="c"># Build and sign the EIF — output will include PCR8 (hash of the signing certificate)</span>
nitro-cli build-enclave <span class="se">\</span>
    <span class="nt">--docker-uri</span> my-enclave-app:latest <span class="se">\</span>
    <span class="nt">--output-file</span> my-enclave-app.eif <span class="se">\</span>
    <span class="nt">--private-key</span> signing-key.pem <span class="se">\</span>
    <span class="nt">--signing-certificate</span> certificate.pem
</code></pre></div></div>

<p>The output now includes <code class="language-plaintext highlighter-rouge">PCR8</code>:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Enclave Image successfully created.
{
  "Measurements": {
    "HashAlgorithm": "Sha384 { ... }",
    "PCR0": "7fb5c55b...efe23e4",
    "PCR1": "235c9e60...cca5286",
    "PCR2": "0f0ac32c...a9ea79d",
    "PCR8": "70da5833...ebbe68f6f"
  }
}
</code></pre></div></div>

<p>These PCR values are the foundation of everything that follows – they are how KMS knows it is talking to <em>your</em> enclave and not an impersonator.</p>

<hr />

<h2 id="platform-configuration-registers-pcrs-and-kms">Platform Configuration Registers (PCRs) and KMS</h2>

<p><a href="https://docs.aws.amazon.com/enclaves/latest/user/set-up-attestation.html#where">Platform Configuration Registers (PCRs)</a> are cryptographic measurements included in the enclave’s attestation document. Each PCR is a SHA384 hash that captures a different aspect of the enclave’s identity:</p>

<table>
  <thead>
    <tr>
      <th>PCR</th>
      <th>Hash of</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>PCR0</td>
      <td>Enclave image file</td>
      <td>A contiguous measure of the contents of the image file. Changes if any code or dependency changes.</td>
    </tr>
    <tr>
      <td>PCR1</td>
      <td>Linux kernel and bootstrap</td>
      <td>A contiguous measurement of the kernel and boot ramfs data.</td>
    </tr>
    <tr>
      <td>PCR2</td>
      <td>Application</td>
      <td>A measurement of the user applications, without the boot ramfs.</td>
    </tr>
    <tr>
      <td>PCR3</td>
      <td>IAM role of the parent instance</td>
      <td>Ensures the parent instance has the expected IAM role.</td>
    </tr>
    <tr>
      <td>PCR4</td>
      <td>Instance ID of the parent instance</td>
      <td>Ties the enclave to a specific EC2 instance.</td>
    </tr>
    <tr>
      <td>PCR8</td>
      <td>Signing certificate</td>
      <td>Ensures the enclave was booted from an image signed by a specific certificate. Only present for signed images.</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p><strong>Important:</strong> Enclaves launched in <code class="language-plaintext highlighter-rouge">--debug-mode</code> produce attestation documents with all PCR values set to zeros. These cannot be used for production attestation.</p>
</blockquote>

<h3 id="using-pcrs-in-kms-key-policies">Using PCRs in KMS Key Policies</h3>

<p>AWS KMS natively supports <a href="https://docs.aws.amazon.com/kms/latest/developerguide/conditions-nitro-enclave.html">condition keys</a> that match against PCR values from the enclave’s attestation document. When an enclave calls a KMS API (like <code class="language-plaintext highlighter-rouge">Decrypt</code> or <code class="language-plaintext highlighter-rouge">GenerateDataKeyPair</code>) with an attestation document, KMS extracts the PCR values and checks them against the conditions in the key policy.</p>

<p>The two most commonly used conditions are:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">kms:RecipientAttestation:ImageSha384</code> – Corresponds to PCR0 (the enclave image digest). This is the recommended shorthand.</li>
  <li><code class="language-plaintext highlighter-rouge">kms:RecipientAttestation:PCR8</code> – The signing certificate hash.</li>
</ul>

<p>Here is an example KMS key policy that restricts <code class="language-plaintext highlighter-rouge">Decrypt</code> and <code class="language-plaintext highlighter-rouge">GenerateDataKeyPair</code> to only be callable from an enclave with the correct image and signing certificate:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"Version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2012-10-17"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"Statement"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"Sid"</span><span class="p">:</span><span class="w"> </span><span class="s2">"AllowKeyAdministration"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"Effect"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Allow"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"Principal"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"AWS"</span><span class="p">:</span><span class="w"> </span><span class="s2">"arn:aws:iam::&lt;ACCOUNT_ID&gt;:root"</span><span class="w">
      </span><span class="p">},</span><span class="w">
      </span><span class="nl">"Action"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="s2">"kms:Create*"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"kms:Describe*"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"kms:Enable*"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"kms:List*"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"kms:Put*"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"kms:Update*"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"kms:Revoke*"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"kms:Disable*"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"kms:Get*"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"kms:Delete*"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"kms:TagResource"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"kms:UntagResource"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"kms:ScheduleKeyDeletion"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"kms:CancelKeyDeletion"</span><span class="w">
      </span><span class="p">],</span><span class="w">
      </span><span class="nl">"Resource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"*"</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"Sid"</span><span class="p">:</span><span class="w"> </span><span class="s2">"AllowEnclaveGenerateDataKeyPair"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"Effect"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Allow"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"Principal"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"AWS"</span><span class="p">:</span><span class="w"> </span><span class="s2">"arn:aws:iam::&lt;ACCOUNT_ID&gt;:role/enclave-instance-role"</span><span class="w">
      </span><span class="p">},</span><span class="w">
      </span><span class="nl">"Action"</span><span class="p">:</span><span class="w"> </span><span class="s2">"kms:GenerateDataKeyPair"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"Resource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"*"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"Condition"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"StringEqualsIgnoreCase"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
          </span><span class="nl">"kms:RecipientAttestation:ImageSha384"</span><span class="p">:</span><span class="w"> </span><span class="s2">"&lt;PCR0_FROM_BUILD_OUTPUT&gt;"</span><span class="p">,</span><span class="w">
          </span><span class="nl">"kms:RecipientAttestation:PCR8"</span><span class="p">:</span><span class="w"> </span><span class="s2">"&lt;PCR8_FROM_BUILD_OUTPUT&gt;"</span><span class="w">
        </span><span class="p">}</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"Sid"</span><span class="p">:</span><span class="w"> </span><span class="s2">"AllowEnclaveDecrypt"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"Effect"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Allow"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"Principal"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"AWS"</span><span class="p">:</span><span class="w"> </span><span class="s2">"arn:aws:iam::&lt;ACCOUNT_ID&gt;:role/enclave-instance-role"</span><span class="w">
      </span><span class="p">},</span><span class="w">
      </span><span class="nl">"Action"</span><span class="p">:</span><span class="w"> </span><span class="s2">"kms:Decrypt"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"Resource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"*"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"Condition"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"StringEqualsIgnoreCase"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
          </span><span class="nl">"kms:RecipientAttestation:ImageSha384"</span><span class="p">:</span><span class="w"> </span><span class="s2">"&lt;PCR0_FROM_BUILD_OUTPUT&gt;"</span><span class="p">,</span><span class="w">
          </span><span class="nl">"kms:RecipientAttestation:PCR8"</span><span class="p">:</span><span class="w"> </span><span class="s2">"&lt;PCR8_FROM_BUILD_OUTPUT&gt;"</span><span class="w">
        </span><span class="p">}</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<blockquote>
  <p>Replace <code class="language-plaintext highlighter-rouge">&lt;ACCOUNT_ID&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;PCR0_FROM_BUILD_OUTPUT&gt;</code>, and <code class="language-plaintext highlighter-rouge">&lt;PCR8_FROM_BUILD_OUTPUT&gt;</code> with the actual values from your enclave build.</p>
</blockquote>

<p>The critical detail: neither <code class="language-plaintext highlighter-rouge">GenerateDataKeyPair</code> nor <code class="language-plaintext highlighter-rouge">Decrypt</code> can be called <strong>without</strong> the attestation conditions being satisfied. The parent EC2 instance uses the same IAM role, but because it cannot produce a valid attestation document, KMS rejects its requests. Only code running inside a genuine Nitro Enclave with the matching image digest and signing certificate can use this key.</p>

<blockquote>
  <p><strong>References:</strong></p>
  <ul>
    <li><a href="https://docs.aws.amazon.com/kms/latest/developerguide/conditions-nitro-enclave.html">Condition keys for Nitro Enclaves</a></li>
    <li><a href="https://docs.aws.amazon.com/enclaves/latest/user/kms.html">Using cryptographic attestation with AWS KMS</a></li>
    <li><a href="https://docs.aws.amazon.com/enclaves/latest/user/set-up-attestation.html#where">Where to get an enclave’s measurements</a></li>
  </ul>
</blockquote>

<hr />

<h2 id="why-not-just-use-kms-directly-generatedatakeypair-explained">Why Not Just Use KMS Directly? GenerateDataKeyPair Explained</h2>

<p>At this point you might be wondering: <em>if the enclave can call KMS, why not just use KMS to sign or encrypt directly?</em> AWS KMS does support <a href="https://docs.aws.amazon.com/kms/latest/APIReference/API_Sign.html">Sign</a> and <a href="https://docs.aws.amazon.com/kms/latest/APIReference/API_Encrypt.html">Encrypt</a> operations on asymmetric KMS keys. You could create an asymmetric KMS key and call <code class="language-plaintext highlighter-rouge">kms:Sign</code> or <code class="language-plaintext highlighter-rouge">kms:Encrypt</code> for every operation.</p>

<p>The problem is that this approach has real drawbacks at scale:</p>

<ul>
  <li><strong>Cost</strong> – Every KMS API call has a price. If your enclave processes thousands of requests per second, the KMS bill adds up fast.</li>
  <li><strong>Latency</strong> – Each <code class="language-plaintext highlighter-rouge">kms:Sign</code> or <code class="language-plaintext highlighter-rouge">kms:Encrypt</code> call is a network round-trip to the KMS service (proxied through the host over vsock). For latency-sensitive workloads, this overhead per operation is significant.</li>
  <li><strong>Availability dependency</strong> – Your enclave’s ability to process requests becomes coupled to KMS availability. If KMS throttles your calls or has a transient issue, your application stalls.</li>
  <li><strong>Control</strong> – With a KMS-managed key, you never hold the private key yourself. That is a feature for some use cases, but a limitation when you need to perform cryptographic operations using libraries or algorithms that KMS does not support natively.</li>
</ul>

<p>The alternative is to <strong>generate the key pair once</strong> via KMS, get the private key securely into the enclave, and then perform all cryptographic operations locally using the in-memory private key – no further KMS calls needed at runtime. This is exactly what <a href="https://docs.aws.amazon.com/kms/latest/APIReference/API_GenerateDataKeyPair.html"><code class="language-plaintext highlighter-rouge">GenerateDataKeyPair</code></a> enables.</p>

<p>This API uses a <strong>symmetric</strong> KMS key (a Customer Master Key, or CMK) to generate an <strong>asymmetric</strong> data key pair. For example, you can generate an ECC key pair (<code class="language-plaintext highlighter-rouge">ECC_NIST_P256</code>) suitable for signing or key-agreement operations outside of KMS.</p>

<p>The response contains:</p>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">PublicKey</code></td>
      <td>The public key in plaintext (DER-encoded).</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">PrivateKeyPlaintext</code></td>
      <td>The private key in plaintext. <strong>Empty when <code class="language-plaintext highlighter-rouge">Recipient</code> is provided.</strong></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">PrivateKeyCiphertextBlob</code></td>
      <td>The private key encrypted under the symmetric KMS CMK. This is the persistent, re-usable envelope.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">CiphertextForRecipient</code></td>
      <td>The private key encrypted under the enclave’s ephemeral RSA public key (from the attestation document). Only present when <code class="language-plaintext highlighter-rouge">Recipient</code> is provided.</td>
    </tr>
  </tbody>
</table>

<p>The key insight here is that when you call <code class="language-plaintext highlighter-rouge">GenerateDataKeyPair</code> with a <code class="language-plaintext highlighter-rouge">Recipient</code> parameter (the enclave’s attestation document), KMS returns <strong>two</strong> encrypted copies of the private key:</p>

<ol>
  <li><strong><code class="language-plaintext highlighter-rouge">PrivateKeyCiphertextBlob</code></strong> – Encrypted under the KMS CMK. This is a durable blob that can be stored and later decrypted by any entity that can satisfy the key policy (i.e., a valid enclave).</li>
  <li><strong><code class="language-plaintext highlighter-rouge">CiphertextForRecipient</code></strong> – Encrypted under the ephemeral RSA public key embedded in the attestation document. This is a one-time transport envelope that only <em>this specific enclave instance</em> can decrypt.</li>
</ol>

<p>The <code class="language-plaintext highlighter-rouge">PrivateKeyPlaintext</code> field is intentionally null – KMS never returns the plaintext private key when a <code class="language-plaintext highlighter-rouge">Recipient</code> is provided.</p>

<blockquote>
  <p><strong>References:</strong></p>
  <ul>
    <li><a href="https://docs.aws.amazon.com/kms/latest/APIReference/API_GenerateDataKeyPair.html">GenerateDataKeyPair API</a></li>
    <li><a href="https://docs.aws.amazon.com/kms/latest/developerguide/data-key-pairs.html">Generate data key pairs</a></li>
    <li><a href="https://docs.aws.amazon.com/kms/latest/developerguide/services-nitro-enclaves.html">Cryptographic attestation support in AWS KMS</a></li>
  </ul>
</blockquote>

<hr />

<h2 id="putting-it-together-encrypt-and-decrypt-files-with-enclave-protected-keys">Putting It Together: Encrypt and Decrypt Files with Enclave-Protected Keys</h2>

<p>With the building blocks in place, let me walk through the architecture I actually implemented. The requirements were:</p>

<ol>
  <li><strong>Scalability</strong> – We needed to run multiple enclave instances behind a load balancer. Every instance had to be able to decrypt the same files, which means they all needed the <strong>same</strong> private key. Generating a new key per enclave would mean each instance had a different identity, and external systems encrypting files would need to know which enclave they were talking to.</li>
  <li><strong>Cost efficiency</strong> – The enclave processes a high volume of requests. Calling KMS for every single sign or decrypt operation was not viable – both from a cost perspective and a latency perspective. We needed the private key available locally in enclave memory so all cryptographic operations could happen without a network round-trip.</li>
  <li><strong>Full key control</strong> – With the private key in memory, we could use any cryptographic library or algorithm supported by our application, not just what KMS offers natively.</li>
  <li><strong>Zero-trust for the host</strong> – Despite all of this, the private key could never be accessible outside the enclave. Not during generation, not during distribution, not at rest.</li>
</ol>

<p>The solution is a pattern I call <strong>“Generate Once, Distribute Encrypted”</strong>: use KMS to generate the key pair exactly once (with the private key delivered securely into the first enclave), store the KMS-encrypted private key blob durably in S3, and let every subsequent enclave recover that same key on boot via attestation-gated KMS <code class="language-plaintext highlighter-rouge">Decrypt</code>.</p>

<h3 id="architecture-overview">Architecture Overview</h3>

<pre><code class="language-mermaid">flowchart TD
    subgraph provisioning ["Phase 1 - Key Provisioning (first boot)"]
        E1["Enclave A"] --&gt;|"GenerateDataKeyPair + Recipient"| KMS1["AWS KMS"]
        KMS1 --&gt;|"PrivateKeyCiphertextBlob"| E1
        E1 --&gt;|"PutObject If-None-Match: *"| S3A["S3 Bucket"]
    end

    subgraph recovery ["Phase 2 - Key Recovery (every subsequent boot)"]
        E2["Enclave B / C / N"] --&gt;|"GetObject ciphertext_blob"| S3B["S3 Bucket"]
        S3B --&gt;|"PrivateKeyCiphertextBlob"| E2
        E2 --&gt;|"Decrypt + Recipient"| KMS2["AWS KMS"]
        KMS2 --&gt;|"CiphertextForRecipient"| E2
        E2 -.-&gt;|"Derive public key in memory"| E2
    end
</code></pre>

<p>All enclaves – regardless of how many instances are running – recover the <strong>same</strong> private key and therefore share the <strong>same</strong> cryptographic identity.</p>

<h3 id="phase-1-key-provisioning">Phase 1: Key Provisioning</h3>

<p>Key provisioning happens when an enclave boots and no encrypted key blob exists at the expected S3 path. This occurs on first deployment or after a key rotation.</p>

<pre><code class="language-mermaid">sequenceDiagram
    participant EC2 as Parent EC2 Instance
    participant Enclave as Nitro Enclave
    participant S3 as S3 Bucket
    participant KMS as AWS KMS

    EC2-&gt;&gt;Enclave: 1. Launch Enclave

    Note over Enclave: 2. Read key_name from&lt;br/&gt;application config&lt;br/&gt;(e.g. "encryption-keys/v1")

    Enclave-&gt;&gt;S3: 3. HeadObject (encryption-keys/v1/ciphertext_blob)
    S3--&gt;&gt;Enclave: 404 Not Found

    Note over Enclave: 4. Generate ephemeral RSA-2048&lt;br/&gt;keypair inside Enclave memory

    Note over Enclave: 5. Request attestation from&lt;br/&gt;Nitro Hypervisor with&lt;br/&gt;ephemeral public key embedded

    Enclave-&gt;&gt;KMS: 6. GenerateDataKeyPair (ECC_NIST_P256)&lt;br/&gt;+ Recipient (attestation document)

    Note right of KMS: KMS validates attestation:&lt;br/&gt;- Genuine Nitro Enclave&lt;br/&gt;- ImageSha384 matches policy&lt;br/&gt;- PCR8 matches policy

    Note right of KMS: KMS generates ECC key pair,&lt;br/&gt;encrypts private key under CMK&lt;br/&gt;(PrivateKeyCiphertextBlob),&lt;br/&gt;re-encrypts for Enclave's&lt;br/&gt;ephemeral RSA key&lt;br/&gt;(CiphertextForRecipient)

    KMS--&gt;&gt;Enclave: PrivateKeyCiphertextBlob + CiphertextForRecipient

    Note over Enclave: 7. Decrypt CiphertextForRecipient&lt;br/&gt;with ephemeral RSA private key&lt;br/&gt;to get ECC private key in memory

    Note over Enclave: 8. Discard ephemeral RSA keypair

    Enclave-&gt;&gt;S3: 9. PutObject (encryption-keys/v1/ciphertext_blob)&lt;br/&gt;with If-None-Match: *

    alt PutObject succeeds (HTTP 200)
        Note over Enclave: 10a. Derive public key from&lt;br/&gt;ECC private key in memory
        Note over Enclave: 11a. Application ready to&lt;br/&gt;encrypt and decrypt files
    else PutObject fails (HTTP 412) -- another Enclave won the race
        Note over Enclave: 10b. Discard generated key pair
        Enclave-&gt;&gt;S3: 11b. GetObject (encryption-keys/v1/ciphertext_blob)
        S3--&gt;&gt;Enclave: PrivateKeyCiphertextBlob from winning Enclave

        Note over Enclave: 12b. Generate NEW ephemeral RSA keypair
        Note over Enclave: 13b. Get fresh attestation

        Enclave-&gt;&gt;KMS: 14b. Decrypt (PrivateKeyCiphertextBlob)&lt;br/&gt;+ Recipient (attestation)
        KMS--&gt;&gt;Enclave: CiphertextForRecipient
        Note over Enclave: 15b. Decrypt CMS envelope&lt;br/&gt;to get same ECC private key
        Note over Enclave: 16b. Application ready&lt;br/&gt;(same identity as all peers)
    end
</code></pre>

<h4 id="race-condition-safety">Race Condition Safety</h4>

<p>When multiple enclaves boot simultaneously (e.g., during auto-scaling), they may all detect that no key exists and attempt to provision one. Without protection, each would generate a different key pair.</p>

<p>The provisioning write uses <a href="https://docs.aws.amazon.com/AmazonS3/latest/userguide/conditional-requests.html">S3 conditional writes</a> (<code class="language-plaintext highlighter-rouge">If-None-Match: *</code>). This instructs S3 to accept the <code class="language-plaintext highlighter-rouge">PutObject</code> <strong>only if the object does not already exist</strong>. If it exists, S3 returns <code class="language-plaintext highlighter-rouge">412 Precondition Failed</code>, and the enclave falls back to recovering the winning enclave’s key.</p>

<p>S3 guarantees that when multiple concurrent <code class="language-plaintext highlighter-rouge">PutObject</code> requests target the same key with <code class="language-plaintext highlighter-rouge">If-None-Match: *</code>, exactly one succeeds and all others are rejected.</p>

<h3 id="phase-2-key-recovery">Phase 2: Key Recovery</h3>

<p>On every subsequent boot, enclaves fetch the encrypted blob and use KMS <code class="language-plaintext highlighter-rouge">Decrypt</code> with the <code class="language-plaintext highlighter-rouge">Recipient</code> flow to unwrap the private key inside their isolated memory:</p>

<pre><code class="language-mermaid">sequenceDiagram
    participant EC2 as Parent EC2 Instance
    participant Enclave as Nitro Enclave
    participant S3 as S3 Bucket
    participant KMS as AWS KMS

    EC2-&gt;&gt;Enclave: 1. Launch Enclave

    Note over Enclave: 2. Read key_name from config

    Enclave-&gt;&gt;S3: 3. GetObject (encryption-keys/v1/ciphertext_blob)
    S3--&gt;&gt;Enclave: PrivateKeyCiphertextBlob (encrypted under CMK)

    Note over Enclave: 4. Generate ephemeral RSA-2048&lt;br/&gt;keypair (unique to this instance)

    Note over Enclave: 5. Request attestation from&lt;br/&gt;Nitro Hypervisor with&lt;br/&gt;ephemeral public key embedded

    Enclave-&gt;&gt;KMS: 6. Decrypt (PrivateKeyCiphertextBlob)&lt;br/&gt;+ Recipient (attestation document)

    Note right of KMS: KMS validates attestation&lt;br/&gt;and decrypts internally.&lt;br/&gt;Plaintext never leaves KMS.&lt;br/&gt;Re-encrypts with THIS&lt;br/&gt;Enclave's ephemeral RSA key.

    KMS--&gt;&gt;Enclave: Plaintext: EMPTY | CiphertextForRecipient

    Note over Enclave: 7. Decrypt CMS envelope with&lt;br/&gt;ephemeral RSA private key&lt;br/&gt;to get same ECC private key&lt;br/&gt;as every other Enclave

    Note over Enclave: 8. Discard ephemeral RSA keypair

    Note over Enclave: 9. Derive public key from&lt;br/&gt;ECC private key

    Note over Enclave: 10. Application ready to&lt;br/&gt;encrypt and decrypt files&lt;br/&gt;(same identity as all peers)
</code></pre>

<p>The critical detail in step 6: when a <code class="language-plaintext highlighter-rouge">Recipient</code> is present in the <code class="language-plaintext highlighter-rouge">Decrypt</code> request, KMS decrypts the blob internally and <strong>does not return the plaintext</strong>. Instead, it re-encrypts the result using the ephemeral RSA public key from the attestation document and returns it as <code class="language-plaintext highlighter-rouge">CiphertextForRecipient</code> – a CMS/PKCS#7 envelope that only this specific enclave instance can open.</p>

<h3 id="using-the-key-for-file-encryption">Using the Key for File Encryption</h3>

<p>Once the enclave has the private key in memory, the workflow for file encryption and decryption is straightforward:</p>

<ul>
  <li><strong>Encrypting a file:</strong> An external system (or the enclave itself) uses the <strong>public key</strong> to encrypt the file. The public key can be freely shared – it is derived from the private key and returned via an API endpoint.</li>
  <li><strong>Decrypting a file:</strong> The encrypted file is sent to the enclave, which uses the <strong>private key</strong> (held only in its volatile memory) to decrypt it. The plaintext is processed inside the enclave and never exposed to the host.</li>
</ul>

<p>Since all enclaves share the same private key, any enclave instance can decrypt files that were encrypted with the corresponding public key. This enables horizontal scaling without key management overhead.</p>

<hr />

<h2 id="why-the-private-key-never-leaves-the-enclave">Why the Private Key Never Leaves the Enclave</h2>

<p>The architecture provides <strong>two independent security layers</strong>:</p>

<h3 id="layer-1--kms-key-policy-attestation-based-access-control">Layer 1 – KMS Key Policy (Attestation-Based Access Control)</h3>

<p>The KMS key policy requires attestation conditions (<code class="language-plaintext highlighter-rouge">ImageSha384</code> and <code class="language-plaintext highlighter-rouge">PCR8</code>) on both <code class="language-plaintext highlighter-rouge">Decrypt</code> and <code class="language-plaintext highlighter-rouge">GenerateDataKeyPair</code>. If the parent EC2 instance, or any other principal, attempts these operations without a valid attestation document from a Nitro Enclave matching the expected PCR values, <strong>KMS rejects the request entirely</strong>. The encrypted blob stored in S3 is useless without passing this gate.</p>

<h3 id="layer-2--recipient-encryption-transport-level-protection">Layer 2 – Recipient Encryption (Transport-Level Protection)</h3>

<p>Every KMS call made by the enclave includes a <code class="language-plaintext highlighter-rouge">Recipient</code> parameter. When KMS processes a request with a <code class="language-plaintext highlighter-rouge">Recipient</code>:</p>

<ol>
  <li>KMS decrypts (or generates) the key material internally.</li>
  <li>KMS <strong>does not</strong> return the plaintext in the API response – the <code class="language-plaintext highlighter-rouge">Plaintext</code> field is always null.</li>
  <li>Instead, KMS re-encrypts the result using the ephemeral RSA public key from the attestation document.</li>
  <li>KMS returns this as <code class="language-plaintext highlighter-rouge">CiphertextForRecipient</code> – a CMS envelope.</li>
</ol>

<p>The enclave communicates with KMS through a vsock proxy on the parent EC2 instance. The host sees every byte of the KMS response, but because the response contains only <code class="language-plaintext highlighter-rouge">CiphertextForRecipient</code> (encrypted to an ephemeral RSA key that exists only in the enclave’s volatile memory), the host cannot extract the private key.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>+------------------------------------------------------------------+
| What the EC2 host sees (proxied over vsock):                     |
|                                                                  |
|   { "Plaintext": null, "CiphertextForRecipient": "&lt;opaque&gt;" }   |
|                                                                  |
|   Cannot decrypt: ephemeral RSA private key exists               |
|   only inside the Enclave's volatile memory                      |
+------------------------------------------------------------------+
</code></pre></div></div>

<h3 id="security-summary">Security Summary</h3>

<table>
  <thead>
    <tr>
      <th>Threat</th>
      <th>Mitigation</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>EC2 host compromise</td>
      <td>Attacker has the encrypted blob but cannot call KMS <code class="language-plaintext highlighter-rouge">Decrypt</code> without Nitro attestation. The <code class="language-plaintext highlighter-rouge">Recipient</code> flow ensures plaintext is never returned.</td>
    </tr>
    <tr>
      <td>S3 bucket access by unauthorized principal</td>
      <td>The blob is encrypted under the KMS CMK. Reading it yields nothing usable without KMS <code class="language-plaintext highlighter-rouge">Decrypt</code> + attestation.</td>
    </tr>
    <tr>
      <td>Network interception (vsock proxy)</td>
      <td>The KMS response contains <code class="language-plaintext highlighter-rouge">CiphertextForRecipient</code>, encrypted to the enclave’s ephemeral RSA key. The host proxies an opaque blob it cannot decrypt.</td>
    </tr>
    <tr>
      <td>Rogue enclave image</td>
      <td>KMS key policy requires <code class="language-plaintext highlighter-rouge">ImageSha384</code> (PCR0) and <code class="language-plaintext highlighter-rouge">PCR8</code> to match. A modified image produces different PCR values and fails attestation.</td>
    </tr>
    <tr>
      <td>Key persistence across reboots</td>
      <td>The private key exists only in volatile enclave memory. On termination it is destroyed. On next boot, the enclave re-derives it via KMS <code class="language-plaintext highlighter-rouge">Decrypt</code> + <code class="language-plaintext highlighter-rouge">Recipient</code> with a fresh ephemeral RSA key.</td>
    </tr>
    <tr>
      <td>Race condition during provisioning</td>
      <td>S3 conditional write (<code class="language-plaintext highlighter-rouge">If-None-Match: *</code>) guarantees exactly one key is persisted. All other enclaves converge on the same key.</td>
    </tr>
    <tr>
      <td>Accidental key overwrite</td>
      <td>The application uses <code class="language-plaintext highlighter-rouge">If-None-Match: *</code> on <code class="language-plaintext highlighter-rouge">PutObject</code>, enforcing immutability at the S3 API level. Key rotation uses a new S3 path, leaving existing blobs untouched.</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="conclusion">Conclusion</h2>

<p>AWS Nitro Enclaves combined with KMS attestation gave us exactly the security model we needed: hardware-backed isolation for a cryptographic private key, with the ability to share that key across a horizontally scalable fleet of enclaves. The “Generate Once, Distribute Encrypted” pattern solved the practical challenge of maintaining a single cryptographic identity across multiple instances without ever exposing the private key outside the enclave boundary.</p>

<p>The core building blocks are:</p>

<ol>
  <li><strong>Nitro Enclaves</strong> for isolated execution with no persistent storage, no network, and no interactive access.</li>
  <li><strong>PCR-based KMS key policies</strong> that ensure only a specific, signed enclave image can access key material.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">GenerateDataKeyPair</code> with <code class="language-plaintext highlighter-rouge">Recipient</code></strong> to create asymmetric keys where the private key is delivered encrypted directly to the enclave.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">Decrypt</code> with <code class="language-plaintext highlighter-rouge">Recipient</code></strong> to recover the private key on subsequent boots, again without ever exposing plaintext outside the enclave.</li>
  <li><strong>S3 conditional writes</strong> to handle race conditions when multiple enclaves provision simultaneously.</li>
</ol>

<p>This pattern is not limited to file encryption. It works for any workload that needs a persistent cryptographic identity inside an ephemeral, isolated compute environment – signing services, token issuers, certificate authorities, or any system where the private key is the crown jewel.</p>

<hr />

<h2 id="references">References</h2>

<table>
  <thead>
    <tr>
      <th>Topic</th>
      <th>Link</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>What is AWS Nitro Enclaves?</td>
      <td><a href="https://docs.aws.amazon.com/enclaves/latest/user/enclaves-user.html">AWS Nitro Enclaves User Guide</a></td>
    </tr>
    <tr>
      <td>Nitro Enclaves concepts</td>
      <td><a href="https://docs.aws.amazon.com/enclaves/latest/user/nitro-enclave-concepts.html">Nitro Enclaves Concepts</a></td>
    </tr>
    <tr>
      <td>Building enclave images</td>
      <td><a href="https://docs.aws.amazon.com/enclaves/latest/user/cmd-nitro-build-enclave.html">nitro-cli build-enclave</a></td>
    </tr>
    <tr>
      <td>Signing enclave images</td>
      <td><a href="https://docs.aws.amazon.com/enclaves/latest/user/cmd-nitro-sign-eif.html">nitro-cli sign-eif</a></td>
    </tr>
    <tr>
      <td>PCR values and attestation</td>
      <td><a href="https://docs.aws.amazon.com/enclaves/latest/user/set-up-attestation.html">Cryptographic attestation</a></td>
    </tr>
    <tr>
      <td>KMS condition keys for enclaves</td>
      <td><a href="https://docs.aws.amazon.com/kms/latest/developerguide/conditions-nitro-enclave.html">Condition keys for Nitro Enclaves</a></td>
    </tr>
    <tr>
      <td>KMS + Nitro Enclaves integration</td>
      <td><a href="https://docs.aws.amazon.com/kms/latest/developerguide/services-nitro-enclaves.html">Cryptographic attestation support in AWS KMS</a></td>
    </tr>
    <tr>
      <td>GenerateDataKeyPair API</td>
      <td><a href="https://docs.aws.amazon.com/kms/latest/APIReference/API_GenerateDataKeyPair.html">GenerateDataKeyPair</a></td>
    </tr>
    <tr>
      <td>Data key pairs guide</td>
      <td><a href="https://docs.aws.amazon.com/kms/latest/developerguide/data-key-pairs.html">Generate data key pairs</a></td>
    </tr>
    <tr>
      <td>S3 conditional writes</td>
      <td><a href="https://docs.aws.amazon.com/AmazonS3/latest/userguide/conditional-requests.html">Conditional requests in Amazon S3</a></td>
    </tr>
    <tr>
      <td>KMS tool sample (enclave + KMS example)</td>
      <td><a href="https://github.com/aws/aws-nitro-enclaves-sdk-c/blob/main/docs/kmstool.md">kmstool on GitHub</a></td>
    </tr>
    <tr>
      <td>Attestation document retriever sample</td>
      <td><a href="https://github.com/aws/aws-nitro-enclaves-samples/blob/main/att_doc_retriever_sample/README.md">att_doc_retriever_sample on GitHub</a></td>
    </tr>
  </tbody>
</table>]]></content><author><name>Rafa Cardoso</name></author><category term="Security" /><category term="aws" /><category term="security" /><category term="Nitro Enclaves" /><category term="KMS" /><category term="cryptography" /><category term="TEE" /><category term="encryption" /><summary type="html"><![CDATA[Recently I was working on a project where we needed to use AWS Nitro Enclaves as a Trusted Execution Environment (TEE) to handle highly sensitive cryptographic keys. The challenge was straightforward to describe but tricky to solve: we needed an asymmetric key pair that multiple enclave instances could share for encrypting and decrypting files, but the private key could never be accessible outside the enclave – not to the host machine, not to operators, not to anyone.]]></summary></entry><entry><title type="html">Speeding up ECS taks startup with Seekable OCI (SOCI)</title><link href="http://localhost:4000/2025/12/26/speed-up-ecs-start/" rel="alternate" type="text/html" title="Speeding up ECS taks startup with Seekable OCI (SOCI)" /><published>2025-12-26T13:00:00+01:00</published><updated>2025-12-26T13:00:00+01:00</updated><id>http://localhost:4000/2025/12/26/speed-up-ecs-start</id><content type="html" xml:base="http://localhost:4000/2025/12/26/speed-up-ecs-start/"><![CDATA[<h2 id="introduction">Introduction</h2>

<p>I recently worked on a real-time communication system that needed to scale quickly during user spikes. We did the usual optimizations—tuning health check intervals, keeping the image small, and trimming container startup paths, but task launch time on <strong>AWS Fargate</strong> was still a bottleneck during rapid scale-out.</p>

<p>That’s when I found <strong>Seekable OCI (SOCI)</strong>: a way to <em>lazy load</em> container images, so a task can start before the full image is downloaded. In this post, I’ll focus on the practical part: how to generate and ship SOCI artifacts reliably in CI/CD (with multi-arch images), and a couple of sharp edges I hit along the way.</p>

<hr />
<h2 id="where-soci-helps-in-the-ecs-task-lifecycle">Where SOCI helps in the ECS task lifecycle</h2>

<p>If you look at the ECS task lifecycle, the part we care about is <code class="language-plaintext highlighter-rouge">ACTIVATING</code>, where the image is pulled from ECR and the container is started (<a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-lifecycle-explanation.html">ECS task lifecycle docs</a>).</p>

<p>With SOCI, the platform can start the container sooner by fetching only the pieces of the image needed for startup. AWS has a great deep dive on how it works under the hood:</p>
<ul>
  <li>Lazy loading on Fargate: <a href="https://aws.amazon.com/blogs/containers/under-the-hood-lazy-loading-container-images-with-seekable-oci-and-aws-fargate/">Under the hood: lazy loading container images with SOCI and AWS Fargate</a></li>
</ul>

<p>In my case, this reduced scale-out latency by ~40% (your results will vary based on image size and what your app touches at boot).</p>

<hr />
<h2 id="soci-index-manifests-v1-vs-v2-and-why-v2-matters">SOCI index manifests: v1 vs v2 (and why v2 matters)</h2>

<p>Before diving into the implementation, it’s worth understanding why there are two versions of SOCI index manifests—and why <strong>this post focuses exclusively on v2</strong>.</p>

<p><strong>SOCI v1</strong> stored the SOCI index as a <em>separate artifact</em> loosely coupled to the container image. This could cause inconsistent deployments: during a rollout, some tasks might lazy load while others fell back to full image downloads if the index was missing or out of sync.</p>

<p><strong>SOCI v2</strong> solves this by binding the image and its SOCI index together via an OCI image index. This improves deployment consistency and makes replication/lifecycle management more reliable. For new implementations, v2 is the clear choice.</p>

<p>AWS has a detailed write-up on the differences:</p>
<ul>
  <li><a href="https://aws.amazon.com/blogs/containers/improving-amazon-ecs-deployment-consistency-with-soci-index-manifest-v2/">Improving Amazon ECS deployment consistency with SOCI Index Manifest v2</a></li>
</ul>

<p>Key requirements for v2:</p>
<ul>
  <li><strong>SOCI CLI v0.10+</strong> (we use v0.12.1 in this post)</li>
  <li><strong>AWS Fargate platform version 1.4.0+</strong></li>
  <li>Generating a SOCI v2 artifact updates the <em>image manifest</em> with annotations, so you’ll see a new image digest even though the filesystem layers don’t change</li>
</ul>

<hr />
<h2 id="cicd-workflow-github-actions-for-multi-arch-images--soci">CI/CD workflow (GitHub Actions) for multi-arch images + SOCI</h2>

<p>My goal was to support <strong>linux/amd64</strong> and <strong>linux/arm64</strong> with one pipeline. Docker Buildx makes multi-platform builds straightforward, but the SOCI CLI operates on images in a <strong>containerd</strong> image store—so you typically end up with this flow:</p>

<pre><code class="language-mermaid">flowchart TD
    A[GitHub Actions Workflow] --&gt; B[Checkout source]
    B --&gt; C[Configure AWS credentials]
    C --&gt; D[Login to Amazon ECR]
    D --&gt; E[Build multi-arch image]
    E --&gt; F[Import into containerd]
    F --&gt; G[Generate SOCI artifact]
    G --&gt; H[Push to ECR]
    H --&gt; I[Deploy to ECS]
</code></pre>

<p>If you want a working reference implementation, AWS provides an end-to-end toolbox (including CI/CD examples):</p>
<ul>
  <li>https://github.com/aws-samples/aws-fargate-seekable-oci-toolbox/tree/main/soci-codepipeline</li>
</ul>

<hr />
<h2 id="the-why-do-i-need-containerdctr-part">The “why do I need containerd/ctr?” part</h2>

<p>The SOCI CLI works with images stored in containerd. In GitHub Actions, that usually means:</p>
<ul>
  <li>Set up containerd</li>
  <li>Build the image with Buildx to an OCI archive (<code class="language-plaintext highlighter-rouge">type=oci,dest=...</code>)</li>
  <li>Import the archive into containerd with <code class="language-plaintext highlighter-rouge">ctr</code></li>
  <li>Generate the SOCI artifact from the containerd image store</li>
  <li>Push the result to ECR</li>
</ul>

<hr />
<h2 id="a-common-auth-pitfall-with-ecr-credentials">A common auth pitfall with ECR credentials</h2>

<p>If you’re following older SOCI examples (based on v1), you might see <code class="language-plaintext highlighter-rouge">soci push</code> used to push the SOCI artifact separately. With <strong>SOCI v2</strong>, this isn’t necessary—<code class="language-plaintext highlighter-rouge">soci convert</code> creates a unified image index, and you push everything with <code class="language-plaintext highlighter-rouge">ctr images push</code>.</p>

<p>However, I initially hit credential errors like:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>soci: failed to fetch list of referrers: ... basic credential not found
</code></pre></div></div>

<p>The fix was ensuring <code class="language-plaintext highlighter-rouge">aws-actions/amazon-ecr-login@v2</code> runs before any SOCI operations:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Login to Amazon ECR</span>
  <span class="na">uses</span><span class="pi">:</span> <span class="s">aws-actions/amazon-ecr-login@v2</span>
</code></pre></div></div>

<p>This action writes valid ECR credentials to <code class="language-plaintext highlighter-rouge">~/.docker/config.json</code>. Even though <code class="language-plaintext highlighter-rouge">ctr images push</code> accepts explicit <code class="language-plaintext highlighter-rouge">--user</code> flags, the SOCI CLI queries the registry during <code class="language-plaintext highlighter-rouge">soci convert</code> and relies on Docker credential helpers. Once the ECR login action runs, everything “just works.”</p>

<hr />
<h2 id="a-minimal-github-actions-example-buildx--containerd--soci-v2--ecr">A minimal GitHub Actions example (Buildx → containerd → SOCI v2 → ECR)</h2>

<p>Below is a simplified version of the pipeline that worked reliably for me using <strong>SOCI index manifest v2</strong> via <strong><code class="language-plaintext highlighter-rouge">soci convert</code></strong>. It’s designed as a reusable workflow—you pass in your ECR registry, repository name, and image tag.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">[Reusable]:</span><span class="nv"> </span><span class="s">ECR</span><span class="nv"> </span><span class="s">Build</span><span class="nv"> </span><span class="s">+</span><span class="nv"> </span><span class="s">Push</span><span class="nv"> </span><span class="s">+</span><span class="nv"> </span><span class="s">SOCI"</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">workflow_call</span><span class="pi">:</span>
    <span class="na">inputs</span><span class="pi">:</span>
      <span class="na">checkout_ref</span><span class="pi">:</span>
        <span class="na">description</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Git</span><span class="nv"> </span><span class="s">ref</span><span class="nv"> </span><span class="s">to</span><span class="nv"> </span><span class="s">checkout</span><span class="nv"> </span><span class="s">(branch,</span><span class="nv"> </span><span class="s">tag,</span><span class="nv"> </span><span class="s">or</span><span class="nv"> </span><span class="s">SHA)"</span>
        <span class="na">required</span><span class="pi">:</span> <span class="no">true</span>
        <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>
      <span class="na">aws_region</span><span class="pi">:</span>
        <span class="na">description</span><span class="pi">:</span> <span class="s2">"</span><span class="s">AWS</span><span class="nv"> </span><span class="s">region"</span>
        <span class="na">required</span><span class="pi">:</span> <span class="no">true</span>
        <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>
      <span class="na">ecr_registry</span><span class="pi">:</span>
        <span class="na">description</span><span class="pi">:</span> <span class="s2">"</span><span class="s">ECR</span><span class="nv"> </span><span class="s">registry,</span><span class="nv"> </span><span class="s">e.g.</span><span class="nv"> </span><span class="s">123.dkr.ecr.us-east-1.amazonaws.com"</span>
        <span class="na">required</span><span class="pi">:</span> <span class="no">true</span>
        <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>
      <span class="na">ecr_repository</span><span class="pi">:</span>
        <span class="na">description</span><span class="pi">:</span> <span class="s2">"</span><span class="s">ECR</span><span class="nv"> </span><span class="s">repository</span><span class="nv"> </span><span class="s">name"</span>
        <span class="na">required</span><span class="pi">:</span> <span class="no">true</span>
        <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>
      <span class="na">image_tag</span><span class="pi">:</span>
        <span class="na">description</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Tag</span><span class="nv"> </span><span class="s">to</span><span class="nv"> </span><span class="s">apply</span><span class="nv"> </span><span class="s">to</span><span class="nv"> </span><span class="s">the</span><span class="nv"> </span><span class="s">image</span><span class="nv"> </span><span class="s">(usually</span><span class="nv"> </span><span class="s">a</span><span class="nv"> </span><span class="s">SHA)"</span>
        <span class="na">required</span><span class="pi">:</span> <span class="no">true</span>
        <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>
      <span class="na">context</span><span class="pi">:</span>
        <span class="na">description</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Docker</span><span class="nv"> </span><span class="s">build</span><span class="nv"> </span><span class="s">context"</span>
        <span class="na">required</span><span class="pi">:</span> <span class="no">false</span>
        <span class="na">default</span><span class="pi">:</span> <span class="s2">"</span><span class="s">./"</span>
        <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>
      <span class="na">dockerfile</span><span class="pi">:</span>
        <span class="na">description</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Dockerfile</span><span class="nv"> </span><span class="s">path"</span>
        <span class="na">required</span><span class="pi">:</span> <span class="no">false</span>
        <span class="na">default</span><span class="pi">:</span> <span class="s2">"</span><span class="s">./Dockerfile"</span>
        <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>
      <span class="na">platforms</span><span class="pi">:</span>
        <span class="na">description</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Comma-separated</span><span class="nv"> </span><span class="s">target</span><span class="nv"> </span><span class="s">platforms"</span>
        <span class="na">required</span><span class="pi">:</span> <span class="no">false</span>
        <span class="na">default</span><span class="pi">:</span> <span class="s2">"</span><span class="s">linux/amd64,linux/arm64"</span>
        <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>
      <span class="na">soci_release</span><span class="pi">:</span>
        <span class="na">description</span><span class="pi">:</span> <span class="s2">"</span><span class="s">SOCI</span><span class="nv"> </span><span class="s">CLI</span><span class="nv"> </span><span class="s">release"</span>
        <span class="na">required</span><span class="pi">:</span> <span class="no">false</span>
        <span class="na">default</span><span class="pi">:</span> <span class="s2">"</span><span class="s">v0.12.1"</span>
        <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>
    <span class="na">secrets</span><span class="pi">:</span>
      <span class="na">role_to_assume</span><span class="pi">:</span>
        <span class="na">required</span><span class="pi">:</span> <span class="no">true</span>
    <span class="na">outputs</span><span class="pi">:</span>
      <span class="na">image_uri</span><span class="pi">:</span>
        <span class="na">description</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Full</span><span class="nv"> </span><span class="s">image</span><span class="nv"> </span><span class="s">URI</span><span class="nv"> </span><span class="s">for</span><span class="nv"> </span><span class="s">the</span><span class="nv"> </span><span class="s">SHA</span><span class="nv"> </span><span class="s">tag"</span>
        <span class="na">value</span><span class="pi">:</span> <span class="s">$</span>

<span class="na">permissions</span><span class="pi">:</span>
  <span class="na">id-token</span><span class="pi">:</span> <span class="s">write</span>
  <span class="na">contents</span><span class="pi">:</span> <span class="s">read</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">build</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">outputs</span><span class="pi">:</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">$</span>
      <span class="na">image_base</span><span class="pi">:</span> <span class="s">$</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v5</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">ref</span><span class="pi">:</span> <span class="s">$</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Configure AWS credentials using OIDC</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">aws-actions/configure-aws-credentials@v4</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">role-to-assume</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">aws-region</span><span class="pi">:</span> <span class="s">$</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Login to Amazon ECR</span>
        <span class="na">id</span><span class="pi">:</span> <span class="s">login-ecr</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">aws-actions/amazon-ecr-login@v2</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Set up containerd</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">crazy-max/ghaction-setup-containerd@v3</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install soci cli</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">lerentis/soci-installer@v1.1.0</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">soci-release</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">install-dir</span><span class="pi">:</span> <span class="s">/usr/local/bin</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Set up Docker Buildx</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">docker/setup-buildx-action@v3</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Determine tags</span>
        <span class="na">id</span><span class="pi">:</span> <span class="s">tag-definition</span>
        <span class="na">shell</span><span class="pi">:</span> <span class="s">bash</span>
        <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s">set -euo pipefail</span>

          <span class="s">IMAGE_BASE="$/$"</span>
          <span class="s">IMAGE="${IMAGE_BASE}:$"</span>

          <span class="s">echo "image=${IMAGE}" &gt;&gt; "$GITHUB_OUTPUT"</span>
          <span class="s">echo "image_base=${IMAGE_BASE}" &gt;&gt; "$GITHUB_OUTPUT"</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Build image (OCI tar)</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">docker/build-push-action@v6</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">context</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">file</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">platforms</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">push</span><span class="pi">:</span> <span class="no">false</span>
          <span class="na">outputs</span><span class="pi">:</span> <span class="s">type=oci,dest=/tmp/image.tar</span>
          <span class="na">tags</span><span class="pi">:</span> <span class="s2">"</span><span class="s">$:local-build"</span>
          <span class="na">cache-from</span><span class="pi">:</span> <span class="s">type=gha</span>
          <span class="na">cache-to</span><span class="pi">:</span> <span class="s">type=gha,mode=max</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Create and push Image and SOCI indexes to ECR</span>
        <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s">ECR_PASSWORD=$(aws ecr get-login-password --region "$")</span>
          <span class="s">IMAGE="$:local-build"</span>
          <span class="s">IMAGE_SOCI="$"</span>

          <span class="s">sudo ctr i import --base-name "${IMAGE}" --digests --all-platforms /tmp/image.tar</span>

          <span class="s"># Generate SOCI index manifest v2 (requires SOCI v0.10+)</span>
          <span class="s"># This creates an OCI index that ties the image + SOCI index together.</span>
          <span class="s">sudo soci convert --all-platforms "${IMAGE}" "${IMAGE_SOCI}"</span>

          <span class="s"># Push the final tag. This publishes the image index plus related manifests/artifacts.</span>
          <span class="s">sudo -E ctr images push --user "AWS:${ECR_PASSWORD}" "${IMAGE_SOCI}"</span>
</code></pre></div></div>

<p>After pushing, reference the final image tag in your ECS task definition. There are no extra flags to “enable” SOCI—Fargate automatically uses lazy loading when the image has a SOCI artifact attached. See the <a href="https://aws.amazon.com/blogs/containers/improving-amazon-ecs-deployment-consistency-with-soci-index-manifest-v2/">AWS v2 blog post</a> for details on how Fargate detects and uses SOCI indexes.</p>

<hr />
<h2 id="closing-thoughts">Closing thoughts</h2>

<p>SOCI was a “low-effort, high-impact” improvement for my workload: no application code changes, but noticeably faster task readiness during scale-out.</p>

<p>If you’re evaluating it, I’d start by ensuring you’re on <strong>SOCI index manifest v2</strong> and validating your workload on the Fargate platform version you run today. From there, the main work is getting CI/CD to build multi-arch images, run SOCI generation in a containerd image store, and push the right artifact to ECR.</p>

<hr />
<h2 id="further-reading">Further reading</h2>

<ul>
  <li><a href="https://aws.amazon.com/blogs/containers/improving-amazon-ecs-deployment-consistency-with-soci-index-manifest-v2/">Improving Amazon ECS deployment consistency with SOCI Index Manifest v2</a> — Deep dive into why v2 exists and how it improves consistency</li>
  <li><a href="https://github.com/aws-samples/aws-fargate-seekable-oci-toolbox/tree/main/soci-codepipeline">AWS Fargate SOCI Toolbox</a> — Reference CI/CD pipelines and IaC templates from AWS</li>
  <li><a href="https://medium.com/@ankit.deo/soci-indexes-for-faster-ecs-deployments-with-amazon-ecr-1bd082032244">SOCI Indexes for Faster ECS Deployments</a> — Another practitioner’s walkthrough</li>
</ul>]]></content><author><name>Rafa Cardoso</name></author><category term="Container" /><category term="aws" /><category term="Container" /><category term="ECS" /><category term="CI/CD" /><category term="ECR" /><category term="scalability" /><category term="Github Action" /><summary type="html"><![CDATA[Introduction]]></summary></entry><entry><title type="html">4 Things I Learned Managing a WebSocket Platform at Scale</title><link href="http://localhost:4000/2025/06/09/websocket-at-scale/" rel="alternate" type="text/html" title="4 Things I Learned Managing a WebSocket Platform at Scale" /><published>2025-06-09T14:00:00+02:00</published><updated>2025-06-09T14:00:00+02:00</updated><id>http://localhost:4000/2025/06/09/websocket-at-scale</id><content type="html" xml:base="http://localhost:4000/2025/06/09/websocket-at-scale/"><![CDATA[<h2 id="introduction">Introduction</h2>

<p>Over the past two years, I’ve managed a real-time platform with WebSocket applications handling more than 8 million open connections and processing over 50,000 RPS.</p>

<p>In this blog post, I want to share 4 key lessons learned while managing this platform. While our infrastructure ran on Kubernetes, these insights apply to any WebSocket deployment on AWS.</p>

<hr />

<h2 id="1-dont-trust-the-aws-ec2-up-to-network-bandwidth">1. Don’t Trust the AWS EC2 “Up to” Network Bandwidth</h2>

<p>When selecting an instance type, you might choose a <code class="language-plaintext highlighter-rouge">c5.2xlarge</code> because it meets your CPU/Memory requirements and advertises “Up to 10 Gbps” network bandwidth. However, AWS documentation clearly states:</p>

<blockquote>
  <p>Instances marked with “Up to” Network Bandwidth have a baseline bandwidth and can use a network I/O credit mechanism to burst beyond their baseline bandwidth on a best effort basis.</p>
</blockquote>

<p>This means you shouldn’t expect to sustain 10 Gbps for extended periods.</p>

<h3 id="finding-the-real-baseline">Finding the Real Baseline</h3>

<p>You can check the actual baseline bandwidth using this AWS CLI command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>aws ec2 describe-instance-types <span class="se">\</span>
    <span class="nt">--filters</span> <span class="s2">"Name=instance-type,Values=c5.*"</span> <span class="se">\</span>
    <span class="nt">--query</span> <span class="s2">"InstanceTypes[].[InstanceType, NetworkInfo.NetworkPerformance, NetworkInfo.NetworkCards[0].BaselineBandwidthInGbps] | sort_by(@,&amp;[2])"</span> <span class="se">\</span>
    <span class="nt">--output</span> table
</code></pre></div></div>

<p>This returns:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>---------------------------------------------
|           DescribeInstanceTypes           |
+--------------+--------------------+-------+
|  c5.large    |  Up to 10 Gigabit  |  0.75 |
|  c5.xlarge   |  Up to 10 Gigabit  |  1.25 |
|  c5.2xlarge  |  Up to 10 Gigabit  |  2.5  |
|  c5.4xlarge  |  Up to 10 Gigabit  |  5.0  |
|  c5.9xlarge  |  12 Gigabit        |  12.0 |
|  c5.12xlarge |  12 Gigabit        |  12.0 |
|  c5.18xlarge |  25 Gigabit        |  25.0 |
|  c5.24xlarge |  25 Gigabit        |  25.0 |
|  c5.metal    |  25 Gigabit        |  25.0 |
+--------------+--------------------+-------+
</code></pre></div></div>

<p>Notice that a <code class="language-plaintext highlighter-rouge">c5.2xlarge</code> has a real baseline of only 2.5 Gbps instead of the advertised 10 Gbps. Choose instance types based on your actual network requirements.</p>

<p>💡 <strong>Tip</strong>: Consider the <code class="language-plaintext highlighter-rouge">n</code> instance variants (like <code class="language-plaintext highlighter-rouge">c5n</code>) which offer better network bandwidth baselines.</p>

<hr />

<h2 id="2-monitor-ec2-network-throttling">2. Monitor EC2 Network Throttling</h2>

<p>Even with the right instance type, you might see packets being queued or dropped. Use <code class="language-plaintext highlighter-rouge">ethtool</code> to investigate network throttling:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>ethtool <span class="nt">-S</span> eth0 | <span class="nb">grep </span>exceeded
    bw_in_allowance_exceeded: 5976
    bw_out_allowance_exceeded: 70727915
    pps_allowance_exceeded: 3316
    conntrack_allowance_exceeded: 0
    linklocal_allowance_exceeded: 0
</code></pre></div></div>

<h3 id="understanding-the-metrics">Understanding the Metrics</h3>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">bw_in_allowance_exceeded</code></strong>:  High values indicate incoming throttling</li>
  <li><strong><code class="language-plaintext highlighter-rouge">bw_out_allowance_exceeded</code></strong>: High values indicate egress throttling</li>
  <li><strong><code class="language-plaintext highlighter-rouge">pps_allowance_exceeded</code></strong>: Too many small packets hitting rate limits</li>
  <li><strong><code class="language-plaintext highlighter-rouge">conntrack_allowance_exceeded</code></strong>: Connection tracking table full (should be 0)</li>
  <li><strong><code class="language-plaintext highlighter-rouge">linklocal_allowance_exceeded</code></strong>: Link-local traffic limit exceeded (should be 0)</li>
</ul>

<p>💡 <strong>Tip</strong>: Monitor these values with CloudWatch and set up alerts. Packet retransmission significantly slows down WebSocket applications.</p>

<hr />

<h2 id="3-tune-conntrack-for-long-lived-connections">3. Tune Conntrack for Long-Lived Connections</h2>

<p>WebSocket applications maintain long-lived connections that can exhaust the Linux connection tracking table, causing new connections to be dropped.</p>

<h3 id="check-current-usage">Check Current Usage</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Current tracked connections</span>
<span class="nb">cat</span> /proc/sys/net/netfilter/nf_conntrack_count

<span class="c"># Maximum allowed connections  </span>
<span class="nb">cat</span> /proc/sys/net/netfilter/nf_conntrack_max

<span class="c"># Usage percentage</span>
<span class="nb">echo</span> <span class="s2">"scale=2; </span><span class="si">$(</span><span class="nb">cat</span> /proc/sys/net/netfilter/nf_conntrack_count<span class="si">)</span><span class="s2"> * 100 / </span><span class="si">$(</span><span class="nb">cat</span> /proc/sys/net/netfilter/nf_conntrack_max<span class="si">)</span><span class="s2">"</span> | bc
</code></pre></div></div>

<h3 id="increase-the-limits-for-high-load-environments">Increase the Limits for High-Load Environments</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># For high-load WebSocket environments, increase significantly</span>
<span class="c"># Rule of thumb: 4× expected concurrent connections, considering CPU cores</span>
<span class="nb">sudo </span>sysctl <span class="nt">-w</span> net.netfilter.nf_conntrack_max<span class="o">=</span>524288

<span class="c"># Make persistent</span>
<span class="nb">echo</span> <span class="s2">"net.netfilter.nf_conntrack_max = 524288"</span> | <span class="nb">sudo tee</span> <span class="nt">-a</span> /etc/sysctl.conf
</code></pre></div></div>

<h3 id="optimize-for-websocket-applications">Optimize for WebSocket Applications</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Reduce timeout for established connections (2 hours instead of 5 days)</span>
<span class="nb">echo</span> <span class="s2">"net.netfilter.nf_conntrack_tcp_timeout_established = 7200"</span> | <span class="nb">sudo tee</span> <span class="nt">-a</span> /etc/sysctl.conf

<span class="c"># Optimize connection cleanup</span>
<span class="nb">echo</span> <span class="s2">"net.netfilter.nf_conntrack_tcp_timeout_time_wait = 30"</span> | <span class="nb">sudo tee</span> <span class="nt">-a</span> /etc/sysctl.conf

<span class="c"># Increase hash table size for better performance with many connections</span>
<span class="nb">echo</span> <span class="s2">"net.netfilter.nf_conntrack_buckets = 65536"</span> | <span class="nb">sudo tee</span> <span class="nt">-a</span> /etc/sysctl.conf
</code></pre></div></div>

<p>💡 <strong>Tip</strong>: Set <code class="language-plaintext highlighter-rouge">nf_conntrack_max</code> based on your instance’s CPU cores and memory. For high-load environments, use: <code class="language-plaintext highlighter-rouge">(CPU cores × 16384)</code> as a starting point, then adjust based on actual usage and available memory.</p>

<hr />

<h2 id="4-avoid-route53-dns-resolution-limits">4. Avoid Route53 DNS Resolution Limits</h2>

<p>Each network interface can only send <a href="https://docs.aws.amazon.com/whitepapers/latest/hybrid-cloud-dns-options-for-vpc/constraints.html#:~:text=Each%20network%20interface%20in%20an,provided%20DNS%20server%20every%20second.">1024 packets per second</a> to the Amazon-provided DNS server. This becomes a bottleneck for applications making frequent external API calls.</p>

<h3 id="kubernetes-solution">Kubernetes Solution</h3>

<p>Deploy <a href="https://kubernetes.io/docs/tasks/administer-cluster/nodelocaldns/">nodelocaldns</a> to cache DNS queries locally and reduce latency. In high-load environments, it’s recommended to enable <code class="language-plaintext highlighter-rouge">force_tcp</code> to avoid issues caused by dropped or timed-out UDP packets, ensuring more reliable DNS resolution.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">ConfigMap</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">nodelocaldns</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">kube-system</span>
<span class="na">data</span><span class="pi">:</span>
  <span class="na">Corefile</span><span class="pi">:</span> <span class="pi">|</span>
    <span class="s">cluster.local:53 {</span>
        <span class="s">errors</span>
        <span class="s">cache {</span>
            <span class="s">success 9984 30</span>
            <span class="s">denial 9984 5</span>
        <span class="s">}</span>
        <span class="s">reload</span>
        <span class="s">loop</span>
        <span class="s">bind 169.254.20.10</span>
        <span class="s">forward . __PILLAR__CLUSTER__DNS__ {</span>
            <span class="s">force_tcp</span>
        <span class="s">}</span>
        <span class="s">prometheus :9253</span>
        <span class="s">health 169.254.20.10:8080</span>
    <span class="s">}</span>
</code></pre></div></div>

<h3 id="ec2-instance-solution">EC2 Instance Solution</h3>

<p>Set up a local DNS cache using bind9:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Install bind9</span>
<span class="nb">sudo </span>yum <span class="nb">install</span> <span class="nt">-y</span> <span class="nb">bind </span>bind-utils

<span class="c"># Configure as caching nameserver</span>
<span class="nb">sudo tee</span> /etc/named.conf <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
options {
    listen-on port 53 { 127.0.0.1; };
    directory "/var/named";
    recursion yes;
    allow-query { localhost; };
    forwarders { 169.254.169.253; };
    forward only;
};
</span><span class="no">EOF

</span><span class="c"># Update /etc/resolv.conf</span>
<span class="nb">echo</span> <span class="s2">"nameserver 127.0.0.1"</span> | <span class="nb">sudo tee</span> /etc/resolv.conf

<span class="c"># Start and enable bind</span>
<span class="nb">sudo </span>systemctl start named
<span class="nb">sudo </span>systemctl <span class="nb">enable </span>named
</code></pre></div></div>

<p>💡 <strong>Tip</strong>: Monitor DNS query patterns and cache hit rates to optimize your caching strategy.</p>

<hr />

<h2 id="conclusion">Conclusion</h2>

<p>Managing WebSocket applications at scale requires understanding these AWS networking limitations:</p>

<ol>
  <li><strong>Know your real network capacity</strong> - Don’t rely on “up to” bandwidth specifications</li>
  <li><strong>Monitor network throttling</strong> - Use <code class="language-plaintext highlighter-rouge">ethtool</code> and set up CloudWatch alerts</li>
  <li><strong>Tune conntrack for long-lived connections</strong> - Scale limits based on connection patterns</li>
  <li><strong>Implement DNS caching</strong> - Avoid Route53 rate limits with local DNS caching</li>
</ol>

<p>These optimizations helped us maintain stable performance with 8M+ concurrent connections during traffic spikes. Always test these changes in staging environments that mirror your production load.</p>

<hr />

<p>In my next post I’ll talk about the challenges of scaling up and down this environment.</p>

<p><em>Have you encountered similar challenges scaling WebSocket applications? Share your experiences in the comments below.</em></p>

<p><em>This blog was written by :brain: + :robot_face:</em></p>]]></content><author><name>Rafa Cardoso</name></author><category term="Network" /><category term="aws" /><category term="network" /><category term="DNS" /><category term="EC2" /><category term="websocket" /><category term="scalability" /><summary type="html"><![CDATA[Introduction]]></summary></entry><entry><title type="html">Welcome to My Tech Blog</title><link href="http://localhost:4000/2025/06/09/welcome-to-my-blog/" rel="alternate" type="text/html" title="Welcome to My Tech Blog" /><published>2025-06-09T14:00:00+02:00</published><updated>2025-06-09T14:00:00+02:00</updated><id>http://localhost:4000/2025/06/09/welcome-to-my-blog</id><content type="html" xml:base="http://localhost:4000/2025/06/09/welcome-to-my-blog/"><![CDATA[<p>Hey there! Welcome to my tech blog. I’m Rafael, a Engineer passionate about cloud architecture and automation. Here, I’ll be sharing insights about AWS,DevOps practices, and emerging technologies.</p>

<h2 id="what-to-expect">What to Expect</h2>

<p>Expect posts covering:</p>
<ul>
  <li>GenAI</li>
  <li>Cloud Architecture &amp; AWS</li>
  <li>DevOps Best Practices</li>
  <li>Infrastructure as Code</li>
  <li>Kubernetes &amp; Containerization</li>
  <li>Performance Optimization</li>
  <li>Security in the Cloud</li>
</ul>

<p>Follow me on <a href="https://linkedin.com/in/rafavinicius">LinkedIn</a> for more tech insights and updates. Looking forward to sharing knowledge and experiences with you!</p>]]></content><author><name>Rafa Cardoso</name></author><category term="general" /><category term="blog" /><summary type="html"><![CDATA[Hey there! Welcome to my tech blog. I’m Rafael, a Engineer passionate about cloud architecture and automation. Here, I’ll be sharing insights about AWS,DevOps practices, and emerging technologies.]]></summary></entry></feed>